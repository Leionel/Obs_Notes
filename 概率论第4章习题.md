
  

## 练习 4-1 习题解析

  

### 1. 赌徒停止赌博时的平均得失钱数

  

*   **习题：** 某赌徒每次押一元赌注，输赢的概率同为 $1/2$。若输了 50 元或赢了 25 元他就停止赌博。问他在停止赌博时，得失钱数平均是多少？

*   **重要知识点/技巧：** 期望的线性性质、**鞅论**（停止时间）。

*   **解答思路：** 假设赌徒初始有 $a$ 元。由于每次输赢的期望为 0，且过程是对称随机游走，这是经典的鞅问题。根据期望的停时定理，**停止时的期望等于初始期望**。如果初始得失钱数期望为 0，那么停止时的平均得失钱数也是 **0**。

  

### 2. 抽样总数的均值和均方

  

*   **习题：** 一个罐子包含编号为 $1, 2, \dots, n$ 美元的 $n$ 个球。我们随机抽取 $k$ 个球并将它们的数量相加。求总数 $T$ 的**均值** $E(T)$ 和**总数平方的均值** $E(T^2)$。

*   **重要知识点/技巧：** 期望的线性性质、不放回抽样、均方 $E(T^2)$ 的计算（分解为自相关项和交叉项）。

*   **解答思路：**

    1.  **均值 $E(T)$：** 设 $T = \sum_{i=1}^k \xi_i$，其中 $\xi_i$ 是第 $i$ 个球的数字。由于是随机抽样，$E(\xi_i) = E(\xi_1) = \frac{1}{n} \sum_{j=1}^n j = \frac{n+1}{2}$。利用期望的线性性质， $E(T) = k E(\xi_1) = \frac{1}{2} k (n+1)$。

    2.  **总数平方的均值 $E(T^2)$：** 将 $E(T^2)$ 分解为 $\sum_{i=1}^k E(\xi_i^2) + \sum_{i \neq j} E(\xi_i \xi_j) = k E(\xi_1^2) + k(k-1) E(\xi_1 \xi_2)$。

        *   $E(\xi_1^2)$ 可由 $\frac{1}{n} \sum j^2$ 计算。

        *   $E(\xi_1 \xi_2)$ 可由 $\frac{1}{n(n-1)} \sum_{i \neq j} ij$ 计算。

  

### 3. 期望利润最大化的进货量

  

*   **习题：** 设某商店每周的需求量 $\xi \sim U$。给定销售、亏损、调剂的利润率，求商店的期望利润达到最大的进货量。

*   **重要知识点/技巧：** **均匀分布**、期望利润函数 $E[R(y)]$ 的构造与优化。

*   **解答思路：**

    1.  设进货量为 $y$。根据需求 $\xi$ 与进货量 $y$ 的关系，写出利润函数 $R(y, \xi)$ 的分段表达式（分别对应供大于求、供不应求等情况）。

    2.  计算期望利润 $E[R(y)] = \int_{10}^{30} R(y, x) f_\xi(x) dx$，其中 $f_\xi(x) = 1/20$。

    3.  对 $E[R(y)]$ 关于 $y$ 求导或分析其性质，找到最大化期望利润的进货量 $y$。

  

### 4. 二维正态分布的极值期望

  

*   **习题：** 设 $(\xi, \eta) \sim N(0, 0, 1, 1, \rho)$，定义 $\zeta = \max\{\xi, \eta\}$。证明：$E(\zeta) = \sqrt{(1-\rho)/\pi}$，并且 $E(\zeta^2) = 1$。

*   **重要知识点/技巧：** 二维正态分布、**恒等式 $\max(a, b) = \frac{a+b}{2} + \frac{|a-b|}{2}$**、正态随机变量绝对值的期望 $E(|X|) = \sigma \sqrt{2/\pi}$。

*   **解答思路：**

    1.  **证明 $E(\zeta)$：** 利用恒等式，$\zeta = \frac{\xi+\eta}{2} + \frac{|\xi-\eta|}{2}$。

        *   由于 $E(\xi+\eta)=0$，所以 $E(\zeta) = \frac{1}{2} E(|\xi-\eta|)$。

        *   由于 $\xi-\eta \sim N(0, 2(1-\rho))$，其标准差 $\sigma = \sqrt{2(1-\rho)}$。

        *   代入 $E(|X|) = \sigma \sqrt{2/\pi}$，得到 $E(\zeta) = \frac{1}{2} \sqrt{2(1-\rho)} \sqrt{2/\pi} = \sqrt{(1-\rho)/\pi}$。

    2.  **证明 $E(\zeta^2)$：** 利用 $(\xi, \eta)$ 和 $(\eta, \xi)$ 同分布的对称性。

        *   $E(\zeta^2) = E(\max\{\xi, \eta\}^2)$。

        *   $2E(\zeta^2) = E(\xi^2) + E(\eta^2)$（这是因为 $a^2 + b^2 = \max(a, b)^2 + \min(a, b)^2$，且 $E(\max^2) = E(\min^2)$ 由于对称性）。

        *   由于 $\xi, \eta$ 都是 $N(0, 1)$，所以 $E(\xi^2)=1, E(\eta^2)=1$。因此 $2E(\zeta^2) = 2$，得出 $E(\zeta^2) = 1$。

  

### 5. 指数分布的特征化

  

*   **习题：** 设 $\eta \sim p$ 是一个非负随机变量。证明：$\eta \sim E(\lambda)$ 的充要条件是：对任意满足 $E(|f'(\eta)|) < \infty$ 且 $\boldsymbol{f(0)=0}$ 的连续可微函数 $f$，有 $E(f'(\eta)) - \lambda E(f(\eta)) = 0$。

*   **重要知识点/技巧：** **指数分布的特征化**、**分部积分**、**微分方程求解**、Fubini 定理（交换积分次序）。

*   **解答思路：**

    1.  **必要性 $(\Rightarrow)$：** 假设 $\eta \sim E(\lambda)$，密度 $p(x) = \lambda e^{-\lambda x}$。

        *   计算 $E(f'(\eta))$ 的积分，利用**分部积分法**。

        *   由于 $f(0)=0$ 和指数衰减特性，边界项 $[f(x) \cdot (-e^{-\lambda x})]_0^\infty$ 为 0，最终得到 $E(f'(\eta)) = \lambda E(f(\eta))$。

    2.  **充分性 $(\Leftarrow)$：** 假设 $E(f'(\eta)) - \lambda E(f(\eta)) = 0$ 成立。

        *   定义 $f(x) = \int_0^x g(t) dt$（其中 $g$ 是满足可积条件的任意连续函数），则 $f'(x) = g(x)$ 且 $f(0)=0$。

        *   将假设条件 $E(g(\eta)) = \lambda E(\int_0^\eta g(t) dt)$ 转化为积分形式 $\int_0^\infty g(t) dF(t) = \lambda \int_0^\infty (\int_0^x g(t) dt) dF(x)$。

        *   利用 Fubini 定理交换积分次序，得到 $\int_0^\infty g(t) p(t) dt = \lambda \int_0^\infty g(t) (1-F(t)) dt$。

        *   由于 $g(t)$ 是任意的，这导致 $p(t) = \lambda (1-F(t))$。

        *   积分 $\int_0^x p(t) dt = \lambda \int_0^x (1-F(t)) dt$ 得到 $F(x) = \lambda \int_0^x (1-F(t)) dt$。

        *   对 $x$ 求导得到微分方程 $F'(x) = \lambda (1-F(x))$，解此微分方程且满足 $F(0)=0$ 的解为 $F(x) = 1 - e^{-\lambda x}$，即指数分布 $E(\lambda)$。

  

### 6. 标准正态分布的特征化

  

*   **习题：** 设 $\eta \sim p$ 是一个实值随机变量。证明：$\eta \sim N(0, 1)$ 的充要条件是：对任意满足 $E(|f'(\eta)|) < \infty$ 的连续可微函数 $f$，有 $E(f'(\eta)) = E(\eta p(\eta))$。

*   **重要知识点/技巧：** **标准正态分布的特征化**、分部积分、正态密度函数的性质。

*   **解答思路：**

    1.  **必要性 $(\Rightarrow)$：** 假设 $\eta \sim N(0, 1)$，密度函数 $p(x) = \frac{1}{\sqrt{2\pi}} e^{-x^2/2}$。

        *   计算 $E(f'(\eta))$ 的积分，利用分部积分法。

        *   利用正态密度函数的性质 $p'(x) = -x p(x)$，通过分部积分可直接证明 $E(f'(\eta)) = E(\eta p(\eta))$。

    2.  **充分性 $(\Leftarrow)$：** 假设条件成立。通常需要利用特征函数 $\varphi(t) = E(e^{it\eta})$。将 $f(\eta)=e^{it\eta}$ 代入假设条件，通过微分方程求解特征函数，最终证明 $\varphi(t) = e^{-t^2/2}$，即标准正态分布 $N(0, 1)$。

  

---

  

## 练习 4-2 习题解析

  

### 1. 错位排列的均值、方差与极限分布

  

*   **习题：** (1). 使用示性函数证明：对于所有 $n \geq 2$，正确匹配对数 $\xi$ 的**均值和方差为 1**。 (2). 证明：当 $n \to \infty$ 时，$\xi$ 的分布列收敛到**泊松分布**。

*   **重要知识点/技巧：** **示性函数**、期望和方差的线性性质、**错位排列概率**、**泊松逼近**。

*   **解答思路：**

    1.  **均值 $E(\xi)$ 和方差 $Var(\xi)$：**

        *   设 $I_k$ 是第 $k$ 封信件匹配的示性函数，则 $\xi = \sum_{k=1}^n I_k$。

        *   $P(I_k=1) = \frac{(n-1)!}{n!} = 1/n$。$E(\xi) = \sum E(I_k) = n \cdot (1/n) = 1$。

        *   $Var(\xi) = E(\xi^2) - (E\xi)^2$。计算 $E(\xi^2) = \sum E(I_k^2) + \sum_{k \neq j} E(I_k I_j)$。

        *   $E(I_k I_j) = P(I_k=1, I_j=1) = P(I_k=1) P(I_j=1|I_k=1) = \frac{1}{n(n-1)}$。

        *   代入计算得 $Var(\xi) = 1 + 2 \binom{n}{2} \frac{1}{n(n-1)} - 1 = 1 + 1 - 1 = 1$。

    2.  **极限分布：** 利用 $\xi$ 的分布列 $P(\xi = r) = \frac{1}{r!} \sum_{i=0}^{n-r} \frac{(-1)^i}{i!}$。当 $n \to \infty$ 时，$\sum_{i=0}^{n-r} \frac{(-1)^i}{i!} \to e^{-1}$。因此 $P(\xi = r) \to e^{-1}/r!$，即收敛于参数 $\lambda=1$ 的泊松分布。

  

### 2. 均匀分布极值的期望和协方差

  

*   **习题：** 设 $\xi, \eta$ 是独立的 $U$ 随机变量。设 $U = \min\{\xi, \eta\}$ 和 $V = \max\{\xi, \eta\}$。计算 $E(U)$ 和 $Cov(U, V)$。

*   **重要知识点/技巧：** 均匀分布的极值分布、**极值变量的协方差**、恒等式 $UV = \xi\eta$。

*   **解答思路：**

    1.  **计算 $E(U)$：**

        *   $U$ 的分布函数 $F_U(u) = P(U \leq u) = 1 - P(U > u) = 1 - P(\xi > u, \eta > u) = 1 - (1-u)^2 = 2u - u^2$。

        *   $U$ 的密度函数 $f_U(u) = F_U'(u) = 2(1-u)$。

        *   $E(U) = \int_0^1 u \cdot 2(1-u) du = 1/3$。

    2.  **计算 $Cov(U, V)$：**

        *   由于 $U+V = \xi+\eta$ 且 $UV = \xi\eta$。

        *   由于 $\xi, \eta$ 独立， $E(UV) = E(\xi\eta) = E(\xi)E(\eta) = (1/2)(1/2) = 1/4$。

        *   $E(V) = E(\xi+\eta) - E(U) = 1 - 1/3 = 2/3$。

        *   $Cov(U, V) = E(UV) - E(U)E(V) = 1/4 - (1/3)(2/3) = 1/4 - 2/9 = 1/36$。

  

### 3. 不相关但非独立性的证明

  

*   **习题：** 设 $\xi, \eta$ 是参数为 $1/2$ 的独立伯努利随机变量。证明：$\xi+\eta$ 和 $|\xi - \eta|$ 虽然**不相关**，但是**不独立**。

*   **重要知识点/技巧：** **协方差与独立性的关系**、计算 $Cov(\cdot, \cdot)$、寻找反例证明非独立性。

*   **解答思路：**

    1.  **不相关性：** 计算 $Cov(\xi + \eta, |\xi - \eta|)$。

        *   由于 $\xi, \eta$ 对称，可计算出 $E\{(\xi + \eta) |\xi - \eta|\} = 1/4 + 1/4 = 1/2$。

        *   $E(\xi + \eta) = 1$，$E(|\xi - \eta|) = 1/2$。

        *   $Cov(\cdot) = 1/2 - 1 \cdot (1/2) = 0$。故不相关。

    2.  **非独立性：** 寻找反例。

        *   $P(\xi + \eta = 0)$ 且 $P(|\xi - \eta| = 0)$。

        *   $P(\xi + \eta = 0, |\xi - \eta| = 0) = P(\xi = 0, \eta = 0) = 1/4$。

        *   $P(\xi + \eta = 0) = 1/4$，$P(|\xi - \eta| = 0) = P(\xi=\eta) = 1/4 + 1/4 = 1/2$。

        *   由于 $1/4 \neq (1/4) \cdot (1/2) = 1/8$，因此它们不独立。

  

### 4. 尾概率的界限

  

*   **习题：** $f(x)$ 是 $(0, \infty)$ 上非负单调不减函数且 $\lim_{x\to\infty} f(x) = \infty$。证明：如果 $E(f(|\xi|)) < \infty$，则**尾概率** $\lim_{x\to+\infty} f(x)P (|\xi| > x) = 0$。

*   **重要知识点/技巧：** **马尔可夫不等式**、**积分收敛的性质**（控制收敛定理的应用）。

*   **解答思路：**

    1.  令 $\eta = f(|\xi|)$。由于 $E(\eta) = E(f(|\xi|)) < \infty$，且 $f(x)$ 是单调不减的。

    2.  利用马尔可夫不等式（Chebyshev 不等式的一种形式）：

        $$P(|\xi| > x) = P(f(|\xi|) \geq f(x)) = P(\eta \geq f(x))$$

    3.  利用期望积分表示法：

        $$f(x) P(|\xi| > x) \leq f(x) \frac{E(\eta I\{\eta \geq f(x)\})}{f(x)} = E(\eta I\{\eta \geq f(x)\})$$

    4.  令 $x \to \infty$。由于 $\lim_{x\to\infty} f(x) = \infty$，事件 $\{\eta \geq f(x)\}$ 的概率测度趋于 0。

    5.  由于 $E(\eta) < \infty$，根据**控制收敛定理**或积分收敛性质，当 $x \to \infty$ 时，$E(\eta I\{\eta \geq f(x)\})$ 趋于 0。

  

### 5. 矩的界限

  

*   **习题：** 设 $P (|\xi| \geq r) \leq 2e^{-r^2/\kappa}$。证明：对任意 $p \geq 1$，**矩** $E(|\xi|^p)$ 存在上界 $p\Gamma(p/2)\kappa^{p/2}$。

*   **重要知识点/技巧：** 矩的尾概率积分公式、**Gamma 函数**、变量代换。

*   **解答思路：**

    1.  利用非负随机变量的矩积分公式：$E(|\xi|^p) = \int_0^\infty p u^{p-1} P(|\xi| > u) du$。

    2.  代入给定的界限：$E(|\xi|^p) \leq \int_0^\infty p u^{p-1} 2e^{-u^2/\kappa} du$。

    3.  令 $v = u^2/\kappa$，则 $u = \sqrt{\kappa v}$，$du = \frac{1}{2}\sqrt{\kappa/v} dv$，$u^{p-1} du = \frac{1}{2} \kappa^{p/2} v^{p/2 - 1/2} dv$。

    4.  代入积分并化简，利用 Gamma 函数的定义 $\Gamma(z) = \int_0^\infty t^{z-1} e^{-t} dt$，最终得到 $E(|\xi|^p) \leq p\Gamma(p/2)\kappa^{p/2}$。

  

### 6. 分位数界限

  

*   **习题：** 设 $\mu, \sigma^2$ 是期望和方差，$\boldsymbol{m_q}$ 是 $q$ 分位数。证明：$\mu - \frac{\sigma}{2q} \leq m_q \leq \mu + \frac{\sigma}{2(1-q)}$。

*   **重要知识点/技巧：** **分位数定义**、Chebyshev 不等式（或马尔可夫不等式应用）、Cauchy-Schwartz 不等式（$E(|\xi - \mu|) \leq \sigma$）。

*   **解答思路：**

    1.  **证明上界 $m_q \leq \mu + \frac{\sigma}{2(1-q)}$：**

        *   由分位数定义 $1-q \leq P(\xi \geq m_q)$。

        *   $P(\xi \geq m_q) = P(\xi - \mu \geq m_q - \mu) \leq \frac{E((\xi - \mu)^+)}{m_q - \mu}$ (类似于马尔可夫不等式)。

        *   利用 $E(|\xi - \mu|) = 2E((\xi - \mu)^+)$ 且 $E(|\xi - \mu|) \leq \sigma$，得出 $1-q \leq \frac{\sigma}{2(m_q - \mu)}$，整理得 $m_q \leq \mu + \frac{\sigma}{2(1-q)}$。

    2.  **证明下界 $m_q \geq \mu - \frac{\sigma}{2q}$：**

        *   由分位数定义 $q \leq P(\xi \leq m_q)$。

        *   $P(\xi \leq m_q) = P(\xi - \mu \leq -( \mu - m_q)) \leq \frac{E((\xi - \mu)^-)}{\mu - m_q}$。

        *   利用 $E(|\xi - \mu|) = 2E((\xi - \mu)^-)$ 且 $E(|\xi - \mu|) \leq \sigma$，得出 $q \leq \frac{\sigma}{2(\mu - m_q)}$，整理得 $m_q \geq \mu - \frac{\sigma}{2q}$。

  

### 7. 二维正态分布的线性变换和条件期望

  

*   **习题：** 设 $(\xi, \eta) \sim N(0, 0, 1, 1, \rho)$。令 $X = \frac{\xi+\eta}{\sqrt{2}}, Y = \frac{\xi-\eta}{\sqrt{2}}$。求 $D(X), D(Y), E(XY)$ 和 $E(X|Y)$；且判断当 $\rho$ 为何值时，$(X, Y)$ 与 $(\xi, \eta)$ 同分布。

*   **重要知识点/技巧：** **二维正态的线性变换**、**独立性 $\iff$ 不相关性**（对正态分布）、**条件期望**。

*   **解答思路：**

    1.  **计算方差和协方差：** 利用 $E(\xi^2) = E(\eta^2) = 1$ 和 $E(\xi\eta) = \rho$。

        *   $D(X) = E(X^2) = \frac{1}{2} E(\xi^2 + 2\xi\eta + \eta^2) = \frac{1}{2} (1 + 2\rho + 1) = 1+\rho$。

        *   $D(Y) = E(Y^2) = \frac{1}{2} E(\xi^2 - 2\xi\eta + \eta^2) = 1-\rho$。

        *   $E(XY) = \frac{1}{2} E(\xi^2 - \eta^2) = \frac{1}{2} (1 - 1) = 0$。

    2.  **条件期望 $E(X|Y)$：** 由于 $(X, Y)$ 是正态随机变量的线性组合，它们服从二维正态分布。又 $E(XY)=0$，说明 $X$ 和 $Y$ **不相关**，因此 $X$ 和 $Y$ **独立**。故 $E(X|Y) = E(X) = 0$。

    3.  **同分布条件：** $(X, Y)$ 与 $(\xi, \eta)$ 同分布要求它们的协方差矩阵相同：

        $$\text{Cov}(X, Y) = \begin{pmatrix} D(X) & E(XY) \\ E(XY) & D(Y) \end{pmatrix} = \begin{pmatrix} 1+\rho & 0 \\ 0 & 1-\rho \end{pmatrix}$$

        $$\text{Cov}(\xi, \eta) = \begin{pmatrix} 1 & \rho \\ \rho & 1 \end{pmatrix}$$

        要使两者相等，必须 $1+\rho = 1$ 且 $1-\rho = 1$，这要求 $\rho=0$。

  

### 8. 联合密度与条件期望

  

*   **习题：** 设二维随机变量 $(\xi, \eta)$ 的联合密度函数 $p(x, y) = 6xI_{\{0<x<y<1\}}(x, y)$。求 $E(\eta), D(\eta), E(\xi\eta), E(\xi|\eta)$。

*   **重要知识点/技巧：** **边缘密度**、**条件密度**、二重积分求期望。

*   **解答思路：**

    1.  **求 $\eta$ 的边缘密度 $p_\eta(y)$：** 对 $x$ 积分 $\int_0^y 6x dx = 3y^2$。所以 $p_\eta(y) = 3y^2 I_{(0, 1)}(y)$。

    2.  **计算 $E(\eta)$ 和 $D(\eta)$：**

        *   $E(\eta) = \int_0^1 y \cdot 3y^2 dy = 3/4$。

        *   $E(\eta^2) = \int_0^1 y^2 \cdot 3y^2 dy = 3/5$。$D(\eta) = 3/5 - (3/4)^2 = 3/80$。

    3.  **计算 $E(\xi\eta)$：** 利用二重积分 $\iint x y \cdot 6x dx dy = \int_0^1 y (\int_0^y 6x^2 dx) dy = 2/5$。

    4.  **计算条件期望 $E(\xi|\eta)$：**

        *   条件密度 $p_{\xi|\eta}(x|y) = \frac{p(x, y)}{p_\eta(y)} = \frac{6x}{3y^2} = \frac{2x}{y^2}$, $0 < x < y$。

        *   $E(\xi|\eta=y) = \int_0^y x \cdot \frac{2x}{y^2} dx = \frac{2}{y^2} [\frac{x^3}{3}]_0^y = \frac{2y}{3}$。

        *   因此 $E(\xi|\eta) = 2\eta/3$。

  

### 9. 随机极值与指数分布

  

*   **习题：** 设 $\xi, \eta$ 和 $\{U_r\}$ 是独立随机变量， $U_r \sim U$。设 $M = \max\{U_1, \dots, U_\eta\}$。证明 $\boldsymbol{\zeta = \xi - M}$ 服从**指数分布**。

    *   注：资料中 $\xi$ 和 $\eta$ 的分布列较为特殊，它们用于保证 $\zeta$ 服从指数分布。

*   **重要知识点/技巧：** **随机计数变量的极值分布**、全期望公式、几何级数。

*   **解答思路：**

    1.  **求 $M$ 的分布函数 $P(M \leq t)$：**

        *   利用全期望公式 $P(M \leq t) = E(P(M \leq t | \eta))$。

        *   由于 $U_r \sim U$ 且独立，给定 $\eta=y$， $P(M \leq t | \eta=y) = P(U_1 \leq t, \dots, U_y \leq t) = t^y$。

        *   代入 $\eta$ 的分布 $P(\eta=y) = \frac{1}{(e-1)y!}$，计算 $E(t^\eta) = \sum_{y=1}^\infty t^y \frac{1}{(e-1)y!} = \frac{1}{e-1} (e^t - 1)$。

        *   因此 $P(M \leq t) = \frac{e^t - 1}{e-1}$。

    2.  **求 $\zeta = \xi - M$ 的分布函数 $P(\zeta \geq z)$：**

        *   利用 $\xi$ 的分布 $P(\xi=x) = (e-1)e^{-x}, x=1, 2, \dots$ 和全期望公式。

        *   通过积分和求和，并利用 $M$ 的分布，最终证明 $P(\zeta \geq z) = e^{-z}$。这证明了 $\zeta$ 服从参数 $\lambda=1$ 的指数分布。

  

### 10. 随机和的停止时间

  

*   **习题：** 设 $\{\xi_r\}$ 独立且均匀分布在 $$ 上。设 $0 < x < 1$ 并定义 $N = \min\{n \geq 1: \xi_1 + \dots + \xi_n > x\}$。证明 $P(N > n) = x^n/n!$，且求 $N$ 的均值和方差。

*   **重要知识点/技巧：** **随机和的停止时间**、全期望公式（条件概率）、**概率递推关系**、**级数求和**（泰勒级数）。

*   **解答思路：**

    1.  **证明 $P(N > n) = x^n/n!$：**

        *   $P(N > n)$ 意味着 $\sum_{i=1}^n \xi_i \leq x$。

        *   利用**全期望公式** $G_n(x) = P(N > n) = \int_0^x G_{n-1}(x-u) du$ (其中 $\xi_1 \sim U$)。

        *   $G_0(x) = P(N > 0) = 1$。

        *   $G_1(x) = \int_0^x G_0(v) dv = x$。

        *   $G_2(x) = \int_0^x G_1(v) dv = x^2/2$。递推可证 $G_n(x) = x^n/n!$。

    2.  **求 $N$ 的均值和方差：**

        *   $E(N) = \sum_{n=0}^\infty P(N > n) = \sum_{n=0}^\infty \frac{x^n}{n!}$。这个级数是 $e^x$ 的泰勒展开式，故 $\boldsymbol{E(N) = e^x}$。

        *   计算 $N$ 的母函数 $G_N(s) = \sum_{n=1}^\infty s^n P(N = n) = (s-1)e^{sx} + 1$。

        *   利用 $Var(N) = G_N''(1) + G_N'(1) - (G_N'(1))^2$，最终求得 $Var(N) = 2xe^x + e^x - e^{2x}$。

  

---

  

## 练习 4-3 习题解析

  

### 1. 负二项分布的母函数

  

*   **习题：** 求负二项分布的母函数。

*   **重要知识点/技巧：** **母函数的定义**、**负二项级数展开**。

*   **解答思路：**

    1.  负二项分布 $P(\xi = k) = \binom{k+r-1}{r-1} p^r (1-p)^k$。

    2.  母函数 $G_\xi(s) = \sum_{k=0}^\infty s^k P(\xi=k) = p^r \sum_{k=0}^\infty \binom{k+r-1}{r-1} (s(1-p))^k$。

    3.  利用负二项级数公式 $\sum_{k=0}^\infty \binom{k+r-1}{r-1} t^k = (1-t)^{-r}$。

    4.  令 $t = s(1-p)$，得到 $G_\xi(s) = p^r (1 - s(1-p))^{-r} = \left(\frac{p}{1 - (1-p)s}\right)^r$。

  

### 2. 二项分布差值概率的计算

  

*   **习题：** 甲乙两人各掷硬币 $n$ 次，利用母函数求甲掷得正面次数 $\xi$ 大于乙掷得正面次数 $\eta$ **$k$ 次**的概率 $P(\xi > \eta + k)$。

*   **重要知识点/技巧：** **独立同分布的差值分布**、**对称性**、母函数的应用。

*   **解答思路：**

    1.  令 $D = \xi - \eta$。由于 $\xi, \eta \sim B(n, 1/2)$ 且独立， $D$ 的分布关于 0 对称：$P(D=m) = P(D=-m)$。

    2.  利用对称性，我们有 $2P(D > k) + P(-k \leq D \leq k) = 1$。

    3.  因此 $P(D > k) = \frac{1 - P(-k \leq D \leq k)}{2}$。

    4.  计算 $P(-k \leq D \leq k) = \sum_{r=-k}^k P(D=r)$。利用 $P(D=r)$ 的组合公式，该概率等于 $\frac{1}{4^n} \sum_{j=n-k}^{n+k} \binom{2n}{j}$。

    5.  代入上式即得最终结果：$P(\xi > \eta + k) = \frac{1}{2} \left(1 - \frac{1}{4^n} \sum_{j=n-k}^{n+k} \binom{2n}{j}\right)$。

  

### 3. 计算特定密度函数的特征函数

  

*   **习题：** 求出以下密度函数的特征函数：(a) $f(x) = \frac{1}{2}e^{-|x|}$；(b) $f(x) = \frac{1}{2}|x|e^{-|x|}$。

*   **重要知识点/技巧：** **特征函数定义**、复数积分计算、拉普拉斯变换。

*   **解答思路：**

    1.  **对于 (a)：** $f(x)$ 是参数为 1 的拉普拉斯分布的密度。

        *   $\phi(t) = \frac{1}{2} \int_{-\infty}^\infty e^{itx} e^{-|x|} dx$。

        *   将积分拆分为 $(-\infty, 0)$ 和 $(0, \infty)$。

        *   $\phi(t) = \frac{1}{2} (\frac{1}{1-it} + \frac{1}{1+it}) = \frac{1}{1+t^2}$。

    2.  **对于 (b)：**

        *   $\phi(t) = \frac{1}{2} \int_{-\infty}^\infty e^{itx} |x| e^{-|x|} dx$。

        *   同样拆分积分，利用分部积分或识别其为 (a) 的特征函数 $\phi_a(t)$ 的二阶矩导数形式。

        *   最终结果为 $\phi(t) = \frac{1-t^2}{(1+t^2)^2}$。

  

### 4. 特征函数的平滑度界限

  

*   **习题：** 设随机变量 $\xi$ 满足 $E(|\xi|^\alpha) < \infty, 0 < \alpha < 1$。则存在常数 $c > 0$，使得其特征函数 $\varphi$ 满足：$|\varphi(t) - \varphi(s)| \leq c|t-s|^\alpha$。

*   **重要知识点/技巧：** **特征函数的连续性**、积分放缩技巧、不等式 $|e^{iu} - 1| \leq 2$ 和 $|e^{iu} - 1| \leq |u|$。

*   **解答思路：**

    1.  写出差值：$|\varphi(t) - \varphi(s)| = |\int (e^{i(t-s)x} - 1) dF(x)| \leq \int |e^{i(t-s)x} - 1| dF(x)$。

    2.  设 $\delta = |t-s|$。将积分区域分解为 $|x|\delta \geq 1/2$ 和 $|x|\delta < 1/2$。

    3.  在 $|x|\delta < 1/2$ 区域，利用 $|e^{iu} - 1| \leq |u|$，积分项 $\leq \delta \int |x| dF(x)$。

    4.  在 $|x|\delta \geq 1/2$ 区域，利用 $|e^{iu} - 1| \leq 2$。

    5.  利用 $E(|\xi|^\alpha) < \infty$ 的条件进行放缩，最终得到 $|\varphi(t) - \varphi(s)| \leq (2^{1+\alpha} + 1) E(|\xi|^\alpha) |t-s|^\alpha$，故 $c = (2^{1+\alpha} + 1) E(|\xi|^\alpha)$。

  

### 5. 特征函数对应的分布函数

  

*   **习题：** 求特征函数 $\boldsymbol{\cos t}$, $\boldsymbol{(\cos t)^2}$, $\boldsymbol{\sin t/t}$ **对应的分布函数**。

*   **重要知识点/技巧：** **特征函数与分布的一一对应**、欧拉公式、三角恒等式、**均匀分布的特征函数**。

*   **解答思路：**

    1.  **$\varphi(t) = \cos t$：** 利用 $\cos t = \frac{e^{it} + e^{-it}}{2}$。对应于离散分布 $\boldsymbol{P(X=1) = 1/2, P(X=-1) = 1/2}$。

    2.  **$\varphi(t) = (\cos t)^2$：** 利用 $\cos^2 t = \frac{1+\cos(2t)}{2} = \frac{1}{2} + \frac{1}{4}e^{i2t} + \frac{1}{4}e^{-i2t}$。对应于离散分布 $\boldsymbol{P(X=0)=1/2, P(X=2)=1/4, P(X=-2)=1/4}$。

    3.  **$\varphi(t) = \sin t/t$：** 这是**均匀分布 $U(-1, 1)$** 的特征函数。

  

### 6. 多元正态的二次型与卡方分布

  

*   **习题：** 设 $n$ 维随机变量 $\boldsymbol{\xi} \sim N(\boldsymbol{\mu}, \boldsymbol{\Sigma})$ 且 $\boldsymbol{\Sigma > 0}$。证明二次型 $\boldsymbol{(\xi-\mu)\Sigma^{-1}(\xi-\mu)^T \sim \chi^2(n)}$。

*   **重要知识点/技巧：** **多元正态分布**、**白化变换**（对角化）、卡方分布的定义。

*   **解答思路：**

    1.  由于 $\boldsymbol{\Sigma > 0}$，存在非奇异矩阵 $C$，使得 $\boldsymbol{\Sigma = C^T C}$。

    2.  定义**白化变换** $Z = (\boldsymbol{\xi} - \boldsymbol{\mu}) C^{-1}$。

    3.  验证 $Z$ 的均值 $E(Z) = 0$，协方差矩阵 $Cov(Z) = C^{-1} \Sigma (C^{-1})^T = I$。

    4.  因此 $Z$ 是 $n$ 个独立标准正态变量 $Z_1, \dots, Z_n$ 组成的向量。

    5.  将二次型代入：$(\boldsymbol{\xi}-\boldsymbol{\mu})\boldsymbol{\Sigma}^{-1}(\boldsymbol{\xi}-\boldsymbol{\mu})^T = (ZC) (C^T C)^{-1} (ZC)^T = Z Z^T$。

    6.  $Z Z^T = \sum_{i=1}^n Z_i^2$，由卡方分布定义，$\sum_{i=1}^n Z_i^2 \sim \chi^2(n)$。

  

### 7. 正态分布的混合与标准正态性

  

*   **习题：** 设 $(\xi, \eta) \sim N(0, 0, 1, 1, \rho)$，随机变量 $(U, V)$ 与 $(\xi, \eta)$ 独立。证明 $\boldsymbol{\zeta = (U\xi + V\eta) / \sqrt{U^2 + 2\rho UV + V^2}}$ 服从**标准正态分布** $N(0, 1)$。

*   **重要知识点/技巧：** **随机变量的混合分布**、**条件特征函数**、全期望公式。

*   **解答思路：**

    1.  计算 $\zeta$ 的特征函数 $\varphi_\zeta(t) = E(e^{it\zeta})$。

    2.  利用**全期望公式**，在给定 $(U, V)$ 的条件下计算条件期望 $\varphi_\zeta(t) = E[E(e^{it\zeta} | U, V)]$。

    3.  给定 $(U, V)$ 时，分子 $W = U\xi + V\eta$ 是二维正态变量的线性组合，仍服从正态分布。

    4.  计算 $W$ 的条件均值 $E(W|U, V)=0$，和条件方差 $D(W|U, V) = U^2 D(\xi) + V^2 D(\eta) + 2UV Cov(\xi, \eta) = U^2 + V^2 + 2\rho UV$。

    5.  代入 $\zeta$ 的分母，可知 $\zeta$ 在给定 $(U, V)$ 时，服从 $N(0, 1)$。

    6.  因此，条件特征函数 $E(e^{it\zeta} | U, V) = e^{-t^2/2}$。

    7.  再对 $(U, V)$ 求期望，得到 $\varphi_\zeta(t) = E(e^{-t^2/2}) = e^{-t^2/2}$，故 $\zeta \sim N(0, 1)$。

  

---

  

## 练习 4-4 习题解析

  

### 1. 基础概念的直观意义和关系

  

*   **习题：** (1). 简述期望、方差、相关系数、条件期望的直观意义。 (2). 简述分布函数、数字特征、特征函数之间的关系。

*   **重要知识点/技巧：** 概率论基础概念、**条件期望是最佳估计**、**特征函数与分布的一一对应**。

*   **解答思路：**

    1.  **直观意义：**

        *   期望：随机变量的平均值。

        *   方差：随机变量集中在其平均值附近的程度（离散度），值越小越集中。

        *   相关系数：两个随机变量线性相关的程度。

        *   条件期望 $E(\xi|\eta)$：在 $\eta$ 已知的情况下，$\xi$ 的**最佳估计**（在二阶矩存在的 $\eta$ 函数中，离 $\xi$ 的距离最近）。

    2.  **关系：**

        *   分布函数刻画统计规律，确定数字特征。

        *   数字特征反应部分统计性质；部分数字特征（如均值和方差）可以唯一确定某些分布（如正态分布）。

        *   **分布函数与特征函数之间存在一一对应关系**。

  

### 2. 均匀分布下最大值指示函数的收敛性

  

*   **习题：** 设 $\xi_k \sim U$ 独立同分布。设 $S_n = \sum_{k=1}^n I_{A_k}$，其中 $A_k = \{\xi_k = \max\{\xi_1, \dots, \xi_k\}\}$。证明 $E(S_n), D(S_n)$ 的渐近性质，且证明 $S_n/\log n$ **依概率收敛于 1**。

*   **重要知识点/技巧：** **示性函数期望和方差计算**、**切比雪夫不等式**、调和级数的渐近性质 $\sum 1/k \sim \log n$。

*   **解答思路：**

    1.  **计算 $E(S_n)$：** 由于 $\xi_1, \dots, \xi_k$ 独立同分布，$\xi_k$ 是最大值的概率 $P(A_k) = 1/k$。

        *   $E(S_n) = \sum_{k=1}^n E(I_{A_k}) = \sum_{k=1}^n 1/k \sim \log n$。

    2.  **计算 $D(S_n)$：** $D(S_n) = E(S_n^2) - (E(S_n))^2$。

        *   $E(S_n^2) = \sum_{k=1}^n E(I_{A_k}) + 2 \sum_{i<j} E(I_{A_i} I_{A_j})$。

        *   $E(I_{A_i} I_{A_j}) = P(A_i \cap A_j) = P(A_i) P(A_j | A_i) = 1/i \cdot 1/j$。

        *   $D(S_n) = E(S_n) - \sum_{i=1}^n 1/i^2$。因此 $\lim_{n\to\infty} D(S_n)/\log n = 1$。

    3.  **依概率收敛：**

        *   根据 Chebyshev 不等式：$P(|\frac{S_n}{\log n} - 1| \geq \epsilon) = P(|S_n - E(S_n)| \geq \epsilon \log n) \leq \frac{D(S_n)}{\epsilon^2 (\log n)^2}$。

        *   由于 $D(S_n) \sim \log n$，所以 $\frac{D(S_n)}{(\log n)^2}$ 趋于 0。因此 $S_n/\log n \xrightarrow{P} 1$。

  

### 3. 联合密度与零相关

  

*   **习题：** 设联合密度函数 $f(x, y) = kI_{\{|y|<x<1\}}$。求 $E(\xi), Var(\xi), E(\eta), Var(\eta), Cov(\xi, \eta), \rho_{\xi\eta}, E(\xi|\eta), E(\eta|\xi)$。

*   **重要知识点/技巧：** **联合密度**、边缘密度、**对称性**、**零相关与非独立性**。

*   **解答思路：**

    1.  **常数 $k$：** 积分区域 $\int_0^1 \int_{-x}^x dy dx = 1$，故 $k=1$。

    2.  **边缘密度：** $p_\xi(x) = 2x I_{(0, 1)}(x)$；$p_\eta(y) = (1-|y|) I_{(-1, 1)}(y)$。

    3.  **矩和协方差：**

        *   $E(\xi) = 2/3, Var(\xi) = 1/18$。

        *   $E(\eta) = 0$ (由对称性)， $Var(\eta) = 1/6$。

        *   $E(\xi\eta) = \int_0^1 x (\int_{-x}^x y dy) dx = 0$。

        *   $Cov(\xi, \eta) = E(\xi\eta) - E(\xi)E(\eta) = 0$。因此 $\boldsymbol{\rho_{\xi\eta} = 0}$。

    4.  **条件期望：**

        *   $E(\xi|\eta=y) = \int_{|y|}^1 x \frac{1}{1-|y|} dx = \frac{1+|y|}{2}$。因此 $E(\xi|\eta) = \frac{1+|\eta|}{2}$。

        *   $E(\eta|\xi=x) = \int_{-x}^x y \frac{1}{2x} dy = 0$ (由对称性)。因此 $E(\eta|\xi) = 0$。

  

### 4. 样本统计量的矩与协方差

  

*   **习题：** 设 $\xi_k$ 独立同分布，三阶中心矩等于零。求 $E(S_n^2), E((\bar{\xi} - \mu)^2), E((\bar{\xi} - \mu)^3)$ 以及 $\boldsymbol{Cov(\bar{\xi}, S_n^2)}$。

*   **重要知识点/技巧：** **样本均值和样本方差的矩**、独立同分布的性质、**三阶中心矩为零**的条件。

*   **解答思路：**

    1.  **均值和方差：** $E(\bar{\xi}) = \mu$， $E((\bar{\xi} - \mu)^2) = Var(\bar{\xi}) = \sigma^2/n$。

    2.  **三阶中心矩：** $E((\bar{\xi} - \mu)^3) = E(X^3) = \frac{1}{n^3} \sum_{i, j, k} E(X_i X_j X_k)$。由于 $\xi_k$ 的三阶中心矩为零，且是独立同分布，所有交叉项期望都为零，因此 $\boldsymbol{E((\bar{\xi} - \mu)^3) = 0}$。

    3.  **样本方差的期望：** $E(S_n^2) = \frac{n-1}{n} \sigma^2$。

    4.  **协方差 $Cov(\bar{\xi}, S_n^2)$：**

        *   展开 $E[\bar{\xi} S_n^2]$，利用 $\bar{\xi} = \mu + X$ 和 $S_n^2$ 的定义。

        *   由于三阶中心矩为零，且 $\xi_k$ 独立同分布，可以证明 $E[X \cdot \frac{1}{n} \sum X_k^2] = 0$。

        *   最终证明 $E[\bar{\xi} S_n^2] = \mu \sigma^2 \frac{n-1}{n}$。

        *   $Cov(\bar{\xi}, S_n^2) = E[\bar{\xi} S_n^2] - E[\bar{\xi}] E[S_n^2] = \mu \sigma^2 \frac{n-1}{n} - \mu (\sigma^2 \frac{n-1}{n}) = \boldsymbol{0}$。

  

### 5. 二维正态下的最佳线性估计

  

*   **习题：** 设 $(\xi, \eta)$ 是二维正态随机变量。求线性函数 $f(x)=ax+b$，使得 $\boldsymbol{\eta - f(\xi)}$ **与 $\xi$ 独立**；且验证 $E(\eta|\xi) = f(\xi)$。

*   **重要知识点/技巧：** **二维正态的独立性 $\iff$ 不相关性**、条件期望是最佳线性估计。

*   **解答思路：**

    1.  **求 $a, b$：**

        *   由于是二维正态，独立性等价于不相关性，要求 $Cov(\eta - f(\xi), \xi) = 0$。

        *   $Cov(\eta - (a\xi+b), \xi) = Cov(\eta, \xi) - a Cov(\xi, \xi) = \rho\sigma_1\sigma_2 - a\sigma_1^2 = 0$。解得 $\boldsymbol{a = \rho\sigma_2/\sigma_1}$。

        *   要求 $E(\eta) = E(f(\xi))$，即 $\mu_2 = a\mu_1 + b$。解得 $\boldsymbol{b = \mu_2 - a\mu_1}$。

    2.  **验证 $E(\eta|\xi) = f(\xi)$：**

        *   利用条件期望的性质：$E(\eta|\xi) = E(\eta - f(\xi)|\xi) + E(f(\xi)|\xi)$。

        *   由于 $\eta - f(\xi)$ 与 $\xi$ 独立，所以 $E(\eta - f(\xi)|\xi) = E(\eta - f(\xi)) = 0$。

        *   $E(f(\xi)|\xi) = f(\xi)$。故 $E(\eta|\xi) = f(\xi)$。

  

### 6. 条件期望的性质与几乎处处相等

  

*   **习题：** 设 $\xi, \eta$ 是两个二阶矩有限的实值随机变量。如果 $\boldsymbol{E(\eta|\xi) = \xi}$ 且 $\boldsymbol{E(\xi|\eta) = \eta}$，则证明 $\boldsymbol{P(\xi = \eta) = 1}$。

*   **重要知识点/技巧：** **条件期望的性质**、均方误差最小性、零方差蕴含几乎处处相等。

*   **解答思路：**

    1.  证明 $E((\xi - \eta)^2) = 0$，即可得出 $P(\xi = \eta) = 1$。

    2.  $E((\xi - \eta)^2) = E(\xi^2) - 2E(\xi\eta) + E(\eta^2)$。

    3.  利用 $E(\eta|\xi) = \xi$，根据塔定理 (Tower Property)， $E(\eta) = E(E(\eta|\xi)) = E(\xi)$。

    4.  利用 $E(\eta|\xi) = \xi$，可得 $E(\xi\eta) = E(E(\xi\eta|\xi)) = E(\xi E(\eta|\xi)) = E(\xi^2)$。

    5.  同理，利用 $E(\xi|\eta) = \eta$，可得 $E(\xi\eta) = E(\eta E(\xi|\eta)) = E(\eta^2)$。

    6.  因此 $E(\xi^2) = E(\xi\eta) = E(\eta^2)$。

    7.  代入方差：$E((\xi - \eta)^2) = E(\xi^2) - 2E(\xi\eta) + E(\eta^2) = E(\xi^2) - 2E(\xi^2) + E(\xi^2) = 0$。

  

### 7. 离散随机变量和的特征函数与均匀分布

  

*   **习题：** $\{\xi_n, n \geq 1\}$ 独立同分布，服从 $B(1, 1/2)$。定义 $\eta_N = \sum_{n=1}^N \xi_n/2^n$。求 $\eta_N$ 的**特征函数** $E(e^{it\eta_N})$ 及其极限 $\varphi(t)$。给出 $\varphi(t)$ 对应的分布函数。

*   **重要知识点/技巧：** **独立和的特征函数乘积性质**、几何级数求和、**均匀分布特征函数**。

*   **解答思路：**

    1.  **计算 $\xi_n/2^n$ 的特征函数：** $E(e^{it\xi_n/2^n}) = \frac{1}{2} e^{it/2^n} + \frac{1}{2} e^{i0} = \frac{1}{2}(1 + e^{it/2^n})$。

    2.  **计算 $\varphi_{\eta_N}(t)$：** 利用独立性， $\varphi_{\eta_N}(t) = \prod_{k=1}^N \frac{1}{2}(1 + e^{it/2^k})$。

    3.  通过三角函数恒等式和化简（涉及 $\sin(t)/t$ 的展开），最终得到 $\varphi_{\eta_N}(t) = \frac{1}{2^N} e^{it(1/2 - 1/2^{N+1})} \frac{\sin(t/2)}{\sin(t/2^{N+1})}$。

    4.  **求极限：** 当 $N \to \infty$ 时，利用 $\lim_{x\to 0} \frac{\sin x}{x} = 1$，得到 $\boldsymbol{\varphi(t) = \lim_{N\to\infty} \varphi_{\eta_N}(t) = \frac{2e^{it/2} \sin(t/2)}{t}}$。

    5.  **对应分布：** $\varphi(t)$ 对应的分布函数是 $$ 上的**均匀分布** $U(0, 1)$。

  

### 8. Cauchy 分布的稳定性

  

*   **习题：** 验证 **Cauchy 分布**的特征函数 $\boldsymbol{\varphi(t) = e^{-|t|}}$；并证明若 $\xi_i$ 服从 Cauchy 分布，则**样本均值** $\boldsymbol{(\sum \xi_i)/n}$ **也服从 Cauchy 分布**。

*   **重要知识点/技巧：** **Cauchy 分布**、特征函数、**稳定性**。

*   **解答思路：**

    1.  **验证特征函数：** 利用傅里叶逆变换 $f(x) = \frac{1}{2\pi} \int e^{-itx} \varphi(t) dt$，通过计算 $\frac{1}{2\pi} \int_{-\infty}^\infty e^{-itx} e^{-|t|} dt$（拆分积分并利用欧拉公式）。最终证明该积分为 $1/\pi(1+x^2)$，即 Cauchy 密度函数。

    2.  **证明样本均值的分布：**

        *   设 $\bar{\xi}_n = (\sum_{i=1}^n \xi_i)/n$。

        *   利用独立性和特征函数的性质， $\bar{\xi}_n$ 的特征函数为 $\varphi_{\bar{\xi}_n}(t) = E(e^{it \bar{\xi}_n}) = \prod_{i=1}^n E(e^{it \xi_i/n})$。

        *   由于 $\varphi_{\xi_i}(t) = e^{-|t|}$，则 $E(e^{it \xi_i/n}) = \varphi_{\xi_i}(t/n) = e^{-|t/n|}$。

        *   $\varphi_{\bar{\xi}_n}(t) = \prod_{i=1}^n e^{-|t/n|} = (e^{-|t/n|})^n = e^{-|t|}$。

        *   由于 $\varphi_{\bar{\xi}_n}(t) = \varphi_{\xi_1}(t)$，即特征函数相同，所以样本均值 $\bar{\xi}_n$ 也服从 Cauchy 分布（**分布的稳定性**）。