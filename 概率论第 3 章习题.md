
### 练习 3-1 习题解析

  

#### 1. 随机变量之和的可测性

  

*   **习题：** 设 $\xi$ 和 $\eta$ 是 $(\Omega, \mathcal{F}, P)$ 上取值于 $(\mathbb{R}, \mathcal{B})$ 的随机变量，证明：$\xi + \eta$ 是随机变量。

*   **重要知识点/技巧：** 随机变量的可测性定义、**有理数的稠密性** ($\mathbb{Q}$ 在 $\mathbb{R}$ 中稠密)、$\sigma$-代数的可数并封闭性。

*   **解答思路：**

    1.  证明 $\xi + \eta$ 是可测的，只需证明对任意 $a \in \mathbb{R}$，事件 $\{\omega : \xi(\omega) + \eta(\omega) < a\}$ 属于 $\mathcal{F}$。

    2.  利用有理数 $\mathbb{Q}$ 的稠密性，将该事件表示为可数并：$$\{\xi + \eta < a\} = \bigcup_{r \in \mathbb{Q}} (\{\xi < r\} \cap \{\eta < a - r\})$$。

    3.  **等式证明：** 证明包含关系：若 $\omega \in \{\xi + \eta < a\}$，则 $\xi(\omega) < a - \eta(\omega)$。存在 $r \in \mathbb{Q}$ 使得 $\xi(\omega) < r < a - \eta(\omega)$，故 $\omega$ 属于右侧可数并。反之，若 $\omega$ 属于右侧，则 $\xi(\omega) + \eta(\omega) < r + (a - r) = a$，故 $\omega$ 属于左侧。

    4.  **可测性验证：** 由于 $\xi, \eta$ 是随机变量，$\{\xi < r\} \in \mathcal{F}$ 且 $\{\eta < a - r\} \in \mathcal{F}$。由于 $\mathcal{F}$ 对交集和可数并封闭，因此 $\{\xi + \eta < a\} \in \mathcal{F}$。

  

#### 2. 分布函数的复合与性质

  

*   **习题：** 设 $F$ 为分布函数，$r$ 为正整数。证明以下函数为分布函数：(a) $F(x)^r$；(b) $1 - \{1 - F(x)\}^r$；(c) $F(x) + \{1 - F(x)\} \log\{1 - F(x)\}$。

*   **重要知识点/技巧：** 分布函数的三个性质（**单调不减**、**右连续性**、**极限** $\lim_{x\to-\infty} = 0, \lim_{x\to+\infty} = 1$）。

*   **解答思路：**

    1.  **(a) $G(x) = F(x)^r$：**

        *   单调性：$F(x)$ 单调不减，幂函数 $t \to t^r$ 在 $$ 上单调递增，故 $G(x)$ 单调不减。

        *   右连续性：$F(x)$ 右连续，幂函数连续，故 $G(x)$ 右连续。

        *   极限：当 $x \to -\infty, G(x) \to 0^r = 0$；当 $x \to +\infty, G(x) \to 1^r = 1$。

    2.  **(b) $H(x) = 1 - \{1 - F(x)\}^r$：**

        *   单调性：$F(x)$ 单调不减 $\Rightarrow 1 - F(x)$ 单调不增 $\Rightarrow \{1 - F(x)\}^r$ 单调不增 $\Rightarrow H(x)$ 单调不减。

        *   右连续性：$F(x)$ 右连续，故 $H(x)$ 右连续。

        *   极限：当 $x \to -\infty, H(x) \to 1 - (1 - 0)^r = 0$；当 $x \to +\infty, H(x) \to 1 - (1 - 1)^r = 1$。

    3.  **(c) $K(x) = F(x) + \{1 - F(x)\} \log\{1 - F(x)\}$：**

        *   定义 $\varphi(u) = u + (1-u) \log(1-u)$，其中 $u = F(x) \in$。

        *   求导 $\varphi'(u) = 1 - \log(1-u) - 1 = - \log(1-u)$。

        *   单调性：当 $u \in (0, 1)$ 时，$\varphi'(u) \geq 0$，故 $\varphi(u)$ 单调不减， $K(x)$ 单调不减。

        *   右连续性：$\varphi(u)$ 连续，$F(x)$ 右连续，故 $K(x)$ 右连续。

        *   极限：当 $x \to -\infty, F(x) \to 0$， $K(x) \to \varphi(0) = 0$；当 $x \to +\infty, F(x) \to 1$， $K(x) \to \varphi(1) = 1$。

  

#### 3. Pareto 型分布的性质

  

*   **习题：** 设连续型随机变量 $\xi$ 的密度函数 $p(x)$ 满足 $p(x) = C x^{-(1+\alpha)}$ ($x \geq 1$)，其他处为 0。 (1) 求常数 $C$； (2) 求概率 $P(\xi > x)$； (3) 求 $\lim_{x\to+\infty} e^{-x} P(\xi > x)$。

*   **重要知识点/技巧：** 密度函数的**归一化条件**、**积分计算**、极限。

*   **解答思路：**

    1.  **(1) 求 $C$：** 利用归一化条件 $\int_1^\infty p(x) dx = 1$。

        $$\int_1^\infty C x^{-(1+\alpha)} dx = C \left[ \frac{x^{-\alpha}}{-\alpha} \right]_1^\infty = C \cdot \frac{1}{\alpha} = 1$$

        故 $C = \alpha$。

    2.  **(2) 求 $P(\xi > x)$：**

        *   若 $x \geq 1$：$P(\xi > x) = \int_x^\infty \alpha t^{-(1+\alpha)} dt = \alpha \left[ \frac{t^{-\alpha}}{-\alpha} \right]_x^\infty = x^{-\alpha}$。

        *   若 $x < 1$：由于 $\xi \geq 1$ 几乎必然， $P(\xi > x) = 1$。

        *   综上，$P(\xi > x) = \min(1, x^{-\alpha})$。

    3.  **(3) 求极限：**

        $$\lim_{x\to\infty} e^{-x} P(\xi > x) = \lim_{x\to\infty} e^{-x} x^{-\alpha}$$

        由于指数函数 $e^{-x}$ 比幂函数 $x^{-\alpha}$ 衰减得更快，极限为 **0**。

  

#### 4. 概率不等式

  

*   **习题：** $\xi$ 和 $\eta$ 是随机变量，$a < b \in \mathbb{R}$。证明：$|P(a < \xi \leq b) - P(a < \eta \leq b)| \leq P(\xi \neq \eta)$。

*   **重要知识点/技巧：** **概率差分分解**、集合的包含关系。

*   **解答思路：**

    1.  定义集合 $A = \{a < \xi \leq b\}, B = \{a < \eta \leq b\}$。

    2.  利用概率差分的恒等式：$P(A) - P(B) = P(A \cap B^c) - P(A^c \cap B)$。

    3.  考虑集合 $(A \cap B^c) \cup (A^c \cap B)$（$A$ 和 $B$ 的对称差）：该集合中的 $\omega$ 满足 $\omega \in A$ 但 $\omega \notin B$，或 $\omega \notin A$ 但 $\omega \in B$。在这些 $\omega$ 下，$\xi(\omega)$ 和 $\eta(\omega)$ 至少有一个满足区间条件，而另一个不满足，因此必有 $\xi(\omega) \neq \eta(\omega)$。

    4.  故 $(A \cap B^c) \cup (A^c \cap B) \subset \{\xi \neq \eta\}$。

    5.  利用单调性放缩：

        $$|P(A) - P(B)| \leq P(A \cap B^c) + P(A^c \cap B) = P((A \cap B^c) \cup (A^c \cap B)) \leq P(\xi \neq \eta)$$。

  

#### 5. Pólya 罐子模型的均匀分布

  

*   **习题：** 一个罐子初始包含 1 白球和 1 黑球。每次从中随机抽取 1 球观其色后将其返回且加入一个同色球。设 $\xi_n$ 表示第 $n$ 抽球后罐子中白球的个数。证明：对任意 $n \geq 1$， $P(\xi_n = 1 + k) = 1/(n+1)$，$k = 0, 1, \dots, n$。

*   **重要知识点/技巧：** **Pólya 罐子模型**、**全概率公式**、**数学归纳法**。

*   **解答思路：**

    1.  **基础情况 ($n=0$)：** 初始时有 1 白球（即 $\xi_0 = 1$）， $P(\xi_0 = 1) = 1 = 1/(0+1)$，成立。

    2.  **归纳假设：** 假设对 $n-1$，结论成立：$P(\xi_{n-1} = 1 + j) = 1/n$，$j = 0, 1, \dots, n-1$。

    3.  **归纳步骤 (n)：** 利用全概率公式计算 $P(\xi_n = 1 + k)$，$k = 0, 1, \dots, n$。$\xi_n = 1 + k$ 只有两种可能路径：

        *   (a) $\xi_{n-1} = 1 + (k-1)$ 且第 $n$ 次抽取白球（白球数增加 1）。

        *   (b) $\xi_{n-1} = 1 + k$ 且第 $n$ 次抽取黑球（白球数不变）。

    4.  利用条件概率：

        $$P(\xi_n = 1 + k) = P(\xi_{n-1} = 1 + (k-1)) \cdot P(\text{白}|\xi_{n-1} = 1+k-1) + P(\xi_{n-1} = 1 + k) \cdot P(\text{黑}|\xi_{n-1} = 1+k)$$

    5.  代入归纳假设和罐子模型中的概率（总球数 $n+1$）：

        $$P(\xi_n = 1 + k) = \frac{1}{n} \cdot \frac{1+(k-1)}{n+1} + \frac{1}{n} \cdot \left(1 - \frac{1+k}{n+1}\right)$$

        $$P(\xi_n = 1 + k) = \frac{1}{n} \cdot \frac{k}{n+1} + \frac{1}{n} \cdot \frac{n+1-(1+k)}{n+1} = \frac{1}{n(n+1)} (k + n-k) = \frac{1}{n+1}$$。

  

#### 6. 生存函数与失效率函数

  

*   **习题：** 设非负随机变量 $\xi$ 的寿命函数 $S(t) = P(\xi \geq t)$，失效率函数 $\lambda(t) = p(t)/S(t)$。 (a) 证明 $S(x) = e^{-\int_0^x \lambda(t) dt}$ 和 $p(x) = \lambda(x) e^{-\int_0^x \lambda(t) dt}$； (b) 若 $\xi$ 服从参数为 $\lambda$ 的指数分布，求 $\lambda(t)$ 和 $S(t)$。

*   **重要知识点/技巧：** **生存函数**、**失效率函数**、**微分方程求解**。

*   **解答思路：**

    1.  **(a) 证明公式：**

        *   由定义 $S(t) = 1 - F(t)$，求导得 $S'(t) = -F'(t) = -p(t)$。

        *   代入 $\lambda(t)$ 定义：$\lambda(t) = \frac{p(t)}{S(t)} = \frac{-S'(t)}{S(t)} = -\frac{d}{dt} \log S(t)$。

        *   积分：在 $[0, x]$ 上积分得 $\log S(x) - \log S(0) = -\int_0^x \lambda(t) dt$。

        *   由于 $\xi$ 非负， $S(0) = P(\xi \geq 0) = 1$， $\log S(0) = 0$。

        *   故 $S(x) = \exp(-\int_0^x \lambda(t) dt)$。

        *   再由 $p(x) = \lambda(x) S(x)$ 得到 $p(x)$ 的表达式。

    2.  **(b) 指数分布的失效率：**

        *   若 $\xi \sim \text{Exp}(\lambda)$，则 $p(x) = \lambda e^{-\lambda x}$，$x \geq 0$。

        *   生存函数 $S(x) = P(\xi \geq x) = e^{-\lambda x}$。

        *   失效率函数 $\lambda(t) = p(t)/S(t) = (\lambda e^{-\lambda t}) / (e^{-\lambda t}) = \boldsymbol{\lambda}$ (常数)。

  

#### 7. 中位数的存在性与集合性质

  

*   **习题：** 设 $F$ 是 $\xi$ 的分布函数。若存在实数 $m$ 使得 $P(\xi < m) \leq 1/2 \leq P(\xi \leq m)$，则称 $m$ 是 $\xi$ 的中位数。 (1) 证明任何随机变量至少有一个中位数； (2) 证明中位数的集合是一个闭区间。

*   **重要知识点/技巧：** **中位数定义**（分位数 $m_{1/2}$）、分布函数的单调性和右连续性、**下确界** $\inf$。

*   **解答思路：**

    1.  **(1) 存在性：**

        *   中位数条件等价于 $F(m-) \leq 1/2 \leq F(m)$。

        *   定义集合 $A = \{x \in \mathbb{R} : F(x) \geq 1/2\}$。由于 $F(+\infty)=1, F(-\infty)=0$， $A$ 非空且有下界。

        *   令 $m = \inf A$。

        *   由 $F$ 的右连续性， $F(m) \geq 1/2$。

        *   对于 $x < m$， $x \notin A$，故 $F(x) < 1/2$。令 $x \uparrow m$，由 $F$ 的单调性，得 $F(m-) \leq 1/2$。

        *   故 $m$ 是一个中位数。

    2.  **(2) 闭区间性质：**

        *   定义 $m_1 = \inf\{m : F(m) \geq 1/2\}$ (最小中位数) 和 $m_2 = \sup\{m : F(m-) \leq 1/2\}$ (最大中位数)。

        *   证明任意 $m \in [m_1, m_2]$ 都是中位数：若 $m \geq m_1$，则 $F(m) \geq F(m_1) \geq 1/2$。若 $m \leq m_2$，则 $F(m-) \leq F(m_2-) \leq 1/2$。因此 $m$ 满足中位数条件。

        *   证明若 $m \notin [m_1, m_2]$ 则不是中位数：若 $m < m_1$，则 $F(m) < 1/2$，不满足条件。若 $m > m_2$，则 $F(m-) > 1/2$，不满足条件。

        *   因此中位数的集合是闭区间 $[m_1, m_2]$（可能退化为一个点）。

  

---

  

## 练习 3-2 习题解析

  

#### 1. 无理数展开的均匀分布

  

*   **习题：** 设 $\{\xi_k, k \geq 1\}$ 是一列独立同分布的随机变量，且 $P(\xi_k = i) = 1/10, i = 0, 1, \dots, 9$。证明：$\xi = \sum_{k=1}^\infty \xi_k 10^{-k}$ 服从**均匀分布** $U(0, 1)$。

*   **重要知识点/技巧：** **十进制展开**、$\pi-\lambda$ 定理、均匀分布的特征。

*   **解答思路：**

    1.  考虑形如 $I = [m 10^{-n}, (m+1) 10^{-n})$ 的区间，其中 $m = 0, 1, \dots, 10^n - 1$。

    2.  $\xi$ 落在该区间等价于 $\xi$ 的前 $n$ 位数字 $\xi_1, \dots, \xi_n$ 恰好等于 $m$ 的十进制表示 $a_1, \dots, a_n$。

    3.  由于 $\xi_k$ 独立同分布，且 $P(\xi_k = a_k) = 1/10$，因此 $P(\xi \in I) = \prod_{k=1}^n P(\xi_k = a_k) = (1/10)^n$。

    4.  该区间的 Lebesgue 测度（长度）也为 $1/10^n$。

    5.  定义 $\mathcal{C}$ 为所有此类区间构成的 $\pi$-系，且 $\sigma(\mathcal{C})$ 是 $$ 上的 Borel $\sigma$-代数 $\mathcal{B}()$。

    6.  定义 $\mu(A) = P(\xi \in A)$ 和 $\nu(A) = \text{Leb}(A)$。由于 $\mu(I) = \nu(I)$ 在 $\pi$-系 $\mathcal{C}$ 上成立，根据 $\pi-\lambda$ 定理， $\mu = \nu$ 在 $\mathcal{B}()$ 上成立。

    7.  因此 $P(\xi \in A) = \text{Leb}(A)$，即 $\xi \sim U(0, 1)$。

  

#### 2. 连续型随机变量的相等性概率

  

*   **习题：** 设 $\xi, \eta$ 是两个一维连续型随机变量。 (1) 给一个例子，满足 $P(\xi = \eta) \neq 0$； (2) 假设 $(\xi, \eta)$ 为二维连续型随机向量，证明 $P(\xi = \eta) = 0$。

*   **重要知识点/技巧：** **连续型随机变量的定义**、**二维连续型随机向量的定义**、Fubini 定理、Lebesgue 测度。

*   **解答思路：**

    1.  **(1) 反例：** 两个随机变量 $\xi$ 和 $\eta$ 具有连续的边缘分布，但它们作为函数可以相等。

        *   取 $\xi \sim N(0, 1)$（标准正态分布），$\eta = \xi$。

        *   $\eta$ 显然也是连续型随机变量（因为它和 $\xi$ 同分布）。

        *   此时 $P(\xi = \eta) = P(\xi = \xi) = 1 \neq 0$。

    2.  **(2) 二维连续型向量：**

        *   若 $(\xi, \eta)$ 是二维连续型随机向量，则存在联合密度 $p(x, y)$。

        *   事件 $\{\xi = \eta\}$ 对应于 $\mathbb{R}^2$ 中的对角线 $x = y$，这是一条直线（二维 Lebesgue 测度为 0 的集合）。

        *   $P(\xi = \eta) = \iint_{x=y} p(x, y) dx dy$。

        *   根据 Fubini 定理，该积分为 0。

  

#### 3. 二维随机向量 $(\xi, \xi)$ 的分布

  

*   **习题：** 设 $\xi$ 是连续型随机变量，密度函数和分布函数分别为 $p(x), F(x)$。求 $(\xi, \xi)$ 的分布函数。问它是否是二维连续型随机向量？

*   **重要知识点/技巧：** **联合分布函数**、连续型随机向量的定义。

*   **解答思路：**

    1.  **求联合分布函数 $G(x, y)$：**

        *   $G(x, y) = P(\xi \leq x, \xi \leq y)$。

        *   事件 $\{\xi \leq x\} \cap \{\xi \leq y\} = \{\xi \leq \min(x, y)\}$。

        *   因此 $G(x, y) = P(\xi \leq \min(x, y)) = \boldsymbol{F(\min(x, y))}$。

    2.  **判断是否为二维连续型随机向量：**

        *   假设 $(\xi, \xi)$ 是二维连续型随机向量，则 $P((\xi, \xi) \in B) = \iint_B g(u, v) du dv$。

        *   考虑对角线 $A = \{(u, v) : u = v\}$。 $P((\xi, \xi) \in A) = P(\xi = \xi) = 1$。

        *   但 $A$ 是 $\mathbb{R}^2$ 中的一条直线，其二维 Lebesgue 测度为 0。如果 $(\xi, \xi)$ 是连续型随机向量，则 $P((\xi, \xi) \in A)$ 应该为 0。

        *   $1 \neq 0$，产生矛盾。因此 **$(\xi, \xi)$ 不是二维连续型随机向量**。

  

#### 4. 分布函数的构造

  

*   **习题：** 设 $F, G$ 是分布函数，$0 < p < 1$。构造随机变量或向量，使得其分布函数是：(1) $F(x)G(x)$；(2) $1 - (1 - F(x)) \lor (1 - G(x))$；(3) $F(x \land y)$；(4) $pF(x) + (1 - p)G(x)$。

*   **重要知识点/技巧：** 独立随机变量的极值分布、**混合分布**、**联合分布的构造**。

*   **解答思路：**

    1.  **(1) $H_1(x) = F(x)G(x)$：**

        *   取 $\xi \sim F, \eta \sim G$，且 $\xi$ 与 $\eta$ **独立**。

        *   令 $\zeta = \max(\xi, \eta)$。则 $P(\zeta \leq x) = P(\xi \leq x, \eta \leq x) = F(x)G(x)$。

    2.  **(2) $H_2(x) = 1 - (1 - F(x)) \lor (1 - G(x))$：**

        *   利用恒等式 $1 - (1 - A) \lor (1 - B) = A \land B$。故 $H_2(x) = \min(F(x), G(x))$。

        *   取 $U \sim U(0, 1)$。令 $\zeta = \inf\{t : \min(F(t), G(t)) \geq U\}$。

        *   则 $P(\zeta \leq x) = P(U \leq \min(F(x), G(x))) = \min(F(x), G(x))$。

    3.  **(3) $H_3(x, y) = F(x \land y) = F(\min(x, y))$：**

        *   取 $\xi \sim F$。令随机向量为 $\boldsymbol{(\xi, \xi)}$。

        *   $P(\xi \leq x, \xi \leq y) = P(\xi \leq \min(x, y)) = F(\min(x, y))$。

    4.  **(4) $H_4(x) = pF(x) + (1 - p)G(x)$：**

        *   取 $B \sim \text{Bernoulli}(p)$，与 $\xi \sim F, \eta \sim G$ 独立。

        *   令 $\zeta = \xi I_{\{B=1\}} + \eta I_{\{B=0\}}$ (混合分布)。

        *   $P(\zeta \leq x) = P(\zeta \leq x | B=1) P(B=1) + P(\zeta \leq x | B=0) P(B=0) = pF(x) + (1-p)G(x)$。

  

#### 5. 泊松分布与二项分布的混合

  

*   **习题：** 设班车起点站上客人数 $\xi \sim \text{Poisson}(\lambda)$。每位乘客中途下车概率为 $p$，独立。设 $\eta$ 表示中途下车人数。 (1) 求 $P(\eta = m | \xi = n)$； (2) 求 $(\xi, \eta)$ 的概率分布。

*   **重要知识点/技巧：** **条件分布**、**二项分布**、全概率公式、**联合概率分布**。

*   **解答思路：**

    1.  **(1) 条件概率 $P(\eta = m | \xi = n)$：**

        *   给定 $\xi = n$（有 $n$ 个乘客），每个乘客以概率 $p$ 下车，故 $\eta$ 服从二项分布 $\text{Binomial}(n, p)$。

        *   $P(\eta = m | \xi = n) = \binom{n}{m} p^m (1 - p)^{n-m}$，$0 \leq m \leq n$。

    2.  **(2) 联合概率 $P(\xi = n, \eta = m)$：**

        *   利用乘法公式：$P(\xi = n, \eta = m) = P(\xi = n) \cdot P(\eta = m | \xi = n)$。

        *   $$P(\xi = n, \eta = m) = \frac{e^{-\lambda} \lambda^n}{n!} \cdot \binom{n}{m} p^m (1 - p)^{n-m}, \quad 0 \leq m \leq n$$。

  

#### 6. 密度函数分解与独立性

  

*   **习题：** 设随机向量 $(\xi, \eta)$ 具有密度函数 $p(x, y) = \frac{1+xy}{4} I_{(-1, 1) \times (-1, 1)}(x, y)$。证明：$\xi$ 和 $\eta$ 不独立，但 $\xi^2$ 和 $\eta^2$ 独立。

*   **重要知识点/技巧：** 边缘密度、**独立性验证**（乘积 $\iff$ 独立）、**随机变量的函数变换**（雅可比行列式）。

*   **解答思路：**

    1.  **$\xi$ 和 $\eta$ 的独立性：**

        *   先求边缘密度 $p_\xi(x)$。对 $x \in (-1, 1)$，$p_\xi(x) = \int_{-1}^1 \frac{1+xy}{4} dy = \frac{1}{4} [y + \frac{xy^2}{2}]_{-1}^1 = \frac{1}{4} (2) = 1/2$。

        *   同理 $p_\eta(y) = 1/2$。

        *   如果 $\xi, \eta$ 独立，则 $p(x, y)$ 应等于 $p_\xi(x) p_\eta(y) = 1/4$。

        *   但 $p(x, y) = (1+xy)/4$。两者仅当 $xy=0$ 成立，这在 $(-1, 1)^2$ 上不普遍成立。因此 **$\xi$ 与 $\eta$ 不独立**。

    2.  **$\xi^2$ 和 $\eta^2$ 的独立性：**

        *   令 $U = \xi^2, V = \eta^2$。使用**变量变换法**求 $(U, V)$ 的联合密度 $f_{U, V}(u, v)$。

        *   变换 $u = x^2, v = y^2$ 在区域 $(-1, 1)^2$ 上有 4 个分支，每个分支的雅可比行列式绝对值为 $1/(4\sqrt{uv})$。

        *   $f_{U, V}(u, v)$ 是 4 个分支贡献之和。由于 $xy$ 在四个分支上取值 $\pm \sqrt{uv}$，$\frac{1+xy}{4} + \frac{1-xy}{4} + \frac{1+xy}{4} + \frac{1-xy}{4} = 1$。

        *   故 $f_{U, V}(u, v) = \frac{1}{4\sqrt{uv}} \cdot 1$，$u, v \in (0, 1)$。

        *   计算边缘密度 $f_U(u) = \int_0^1 \frac{1}{4\sqrt{uv}} dv = \frac{1}{2\sqrt{u}}$。同理 $f_V(v) = \frac{1}{2\sqrt{v}}$。

        *   由于 $f_{U, V}(u, v) = f_U(u) f_V(v)$，因此 **$U = \xi^2$ 与 $V = \eta^2$ 独立**。

  

#### 7. 条件密度函数

  

*   **习题：** 当 $(\xi, \eta)$ 具有下面联合密度函数时，求在给定 $\xi$ 下，$\eta$ 的条件密度函数 $f_{\eta|\xi}(y|x)$。 (1) $f(x, y) = \lambda^2 e^{-\lambda y}$, $0 \leq x \leq y < \infty$； (2) $f(x, y) = x e^{-x(y+1)}$, $x, y \geq 0$。

*   **重要知识点/技巧：** **边缘密度**、**条件密度** $f_{\eta|\xi}(y|x) = f(x, y) / f_\xi(x)$。

*   **解答思路：**

    1.  **(1) $f(x, y) = \lambda^2 e^{-\lambda y}$：**

        *   边缘密度 $f_\xi(x) = \int_x^\infty \lambda^2 e^{-\lambda y} dy = \lambda^2 [\frac{e^{-\lambda y}}{-\lambda}]_x^\infty = \lambda e^{-\lambda x}$，$x \geq 0$。

        *   条件密度 $f_{\eta|\xi}(y|x) = \frac{\lambda^2 e^{-\lambda y}}{\lambda e^{-\lambda x}} = \boldsymbol{\lambda e^{-\lambda (y - x)}}$，$y \geq x$。

    2.  **(2) $f(x, y) = x e^{-x(y+1)}$：**

        *   边缘密度 $f_\xi(x) = \int_0^\infty x e^{-x(y+1)} dy = x e^{-x} \int_0^\infty e^{-xy} dy = x e^{-x} \cdot \frac{1}{x} = e^{-x}$，$x > 0$。

        *   条件密度 $f_{\eta|\xi}(y|x) = \frac{x e^{-x(y+1)}}{e^{-x}} = \boldsymbol{x e^{-xy}}$，$y \geq 0$。

  

#### 8. 标准正态随机变量的线性组合

  

*   **习题：** 设 $\xi$ 和 $\eta$ 独立且都服从标准正态分布 $N(0, 1)$。求 $(\xi + \eta, \xi - \eta)$ 的联合密度和边缘密度。

*   **重要知识点/技巧：** **正态分布的线性变换**、**雅可比行列式**、**独立性验证**。

*   **解答思路：**

    1.  定义变换 $U = \xi + \eta, V = \xi - \eta$。逆变换 $\xi = (U+V)/2, \eta = (U-V)/2$。

    2.  计算雅可比行列式 $J = \det \begin{pmatrix} 1/2 & 1/2 \\ 1/2 & -1/2 \end{pmatrix} = -1/2$。

    3.  联合密度 $f_{U, V}(u, v) = f_{\xi, \eta}(\xi(u, v), \eta(u, v)) \cdot |J|$。

        *   由于 $\xi, \eta$ 独立 $N(0, 1)$， $f_{\xi, \eta}(x, y) = \frac{1}{2\pi} e^{-(x^2 + y^2)/2}$。

        *   代入 $x^2 + y^2 = (\frac{u+v}{2})^2 + (\frac{u-v}{2})^2 = \frac{u^2 + v^2}{2}$。

        *   $$f_{U, V}(u, v) = \frac{1}{2\pi} e^{-(u^2+v^2)/4} \cdot \frac{1}{2} = \frac{1}{4\pi} e^{-(u^2+v^2)/4}$$。

    4.  **边缘密度：** 联合密度可以分解 $f_{U, V}(u, v) = [\frac{1}{\sqrt{4\pi}} e^{-u^2/4}] \cdot [\frac{1}{\sqrt{4\pi}} e^{-v^2/4}]$。

        *   $U \sim N(0, 2)$， $f_U(u) = \frac{1}{\sqrt{4\pi}} e^{-u^2/4}$。

        *   $V \sim N(0, 2)$， $f_V(v) = \frac{1}{\sqrt{4\pi}} e^{-v^2/4}$。

        *   **结论：** $U = \xi + \eta$ 和 $V = \xi - \eta$ 独立，且都服从 $N(0, 2)$。

  

#### 9. 标准正态变量的代数组合

  

*   **习题：** 设 $\xi$ 和 $\eta$ 独立都服从标准正态分布 $N(0, 1)$。求 $\eta + \xi, \eta - \xi, \eta\xi$ 和 $\eta/\xi$ 的分布密度。

*   **重要知识点/技巧：** **正态分布的可加性**、**乘积分布**（贝塞尔函数）、**比值分布**（Cauchy 分布）。

*   **解答思路：**

    1.  **$\eta + \xi$ 的分布：** 利用正态分布的可加性，$ \eta + \xi \sim N(0, 1+1) = N(0, 2)$。

        *   密度函数 $f_{\eta+\xi}(u) = \frac{1}{\sqrt{4\pi}} e^{-u^2/4}$。

    2.  **$\eta - \xi$ 的分布：** $\eta - \xi \sim N(0, 1+1) = N(0, 2)$。

        *   密度函数 $f_{\eta-\xi}(v) = \frac{1}{\sqrt{4\pi}} e^{-v^2/4}$。

    3.  **$\eta\xi$ 的分布：** (乘积分布 $W = \eta\xi$)

        *   通过积分变换和公式，最终密度函数为 $f_{\eta\xi}(w) = \frac{1}{\pi} K_0(|w|)$，其中 $K_0$ 是修正贝塞尔函数。

    4.  **$\eta/\xi$ 的分布：** (比值分布 $Z = \eta/\xi$)

        *   两个独立标准正态变量的比值服从**柯西分布** (Cauchy Distribution)。

        *   密度函数 $f_{\eta/\xi}(z) = \frac{1}{\pi (1 + z^2)}$。

  

#### 10. 均匀分布下的最远距离

  

*   **习题：** 在 $$ 上随机等可能地选取 $n$ 个点 $\xi_1, \dots, \xi_n$。求其中**最远两点的距离大于 $1/2$** 的概率。

*   **重要知识点/技巧：** **次序统计量**、**联合密度**、概率积分计算。

*   **解答思路：**

    1.  定义次序统计量 $\xi_{(1)} = \min\{\xi_i\}, \xi_{(n)} = \max\{\xi_i\}$。

    2.  最远两点距离 $D_n = \xi_{(n)} - \xi_{(1)}$。所求概率为 $P(D_n > 1/2) = 1 - P(D_n \leq 1/2)$。

    3.  $\xi_{(1)}$ 和 $\xi_{(n)}$ 的联合密度函数是 $f_{\xi_{(1)}, \xi_{(n)}}(x, y) = n(n-1)(y-x)^{n-2}$, $0 \leq x \leq y \leq 1$。

    4.  计算 $P(D_n \leq a) = P(\xi_{(n)} - \xi_{(1)} \leq a)$。积分区域是 $0 \leq x \leq y \leq 1$ 且 $y - x \leq a$。

    5.  计算二重积分 $\int \int_{y-x \leq a} f_{\xi_{(1)}, \xi_{(n)}}(x, y) dx dy = n a^{n-1} (1-a) + a^n$。

    6.  代入 $a = 1/2$：

        $$P(D_n \leq 1/2) = n (1/2)^{n-1} (1/2) + (1/2)^n = (n+1) (1/2)^n$$。

    7.  所求概率为 $P(D_n > 1/2) = \boldsymbol{1 - \frac{n+1}{2^n}}$。

  

#### 11. 均匀分布的二进制展开与构造

  

*   **习题：** 设 $\xi \sim U[0, 1)$， $\{\xi_n, n \geq 1\}$ 是 $\xi$ 的二进制展开系数。定义 $\eta_n = \sum_{k=1}^\infty \xi_{2^n(2k+1)} 2^{-k}$。 (1) 证明 $\{\xi_n, n \geq 1\}$ IID 且 $\sim B(1, 1/2)$； (2) 证明 $\{\eta_n, n \geq 1\}$ IID 且 $\sim U[0, 1)$； (3) 用 $\{\xi_n\}$ 构造一个二维正态变量 $(\eta, \zeta) \sim N(0, 0, 1, 1, \rho)$； (4) 用 $\{\xi_n\}$ 构造 IID 的指数分布列 $\{\zeta_n, n \geq 1\}$。

*   **重要知识点/技巧：** **二进制展开**、**IID 的构造**、**概率积分变换**、**Box-Muller 变换** (构造正态分布)。

*   **解答思路：**

    1.  **(1) $\{\xi_n\}$ 的分布和独立性：**

        *   $P(\xi_n = 1) = 1/2, P(\xi_n = 0) = 1/2$，即 $\xi_n \sim B(1, 1/2)$。

        *   由 $\xi_1, \dots, \xi_n$ 的联合事件与 $\xi$ 落在长度为 $1/2^n$ 的区间相对应，证明 $P(\xi_1 = i_1, \dots, \xi_n = i_n) = 1/2^n = \prod P(\xi_k = i_k)$，故 $\{\xi_n\}$ IID。

    2.  **(2) $\{\eta_n\}$ 的分布和独立性：**

        *   由于 $\eta_n$ 仅使用 $\{\xi_k\}$ 中的不重复子序列（例如 $\eta_1$ 使用 $\xi_{6}, \xi_{10}, \xi_{14}, \dots$；$\eta_2$ 使用 $\xi_{12}, \xi_{20}, \xi_{28}, \dots$），且这些子序列 IID 且具有与 $\xi$ 相同的结构。

        *   因此 $\{\eta_n\}$ IID，且 $\eta_n \sim U[0, 1)$。

    3.  **(3) 构造二维正态：**

        *   取 $U = \sum_{k=1}^\infty \xi_{2k} 2^{-k}$ 和 $V = \sum_{k=1}^\infty \xi_{2k+1} 2^{-k}$。利用子序列 IID， $U, V$ 独立且 $\sim U(0, 1)$。

        *   令 $\xi = \Phi^{-1}(U), \eta = \Phi^{-1}(V)$。 $(\xi, \eta) \sim N(0, 0, 1, 1, 0)$。

        *   对于 $\rho \in (-1, 1)$，构造 $\Sigma = \begin{pmatrix} 1 & \rho \\ \rho & 1 \end{pmatrix}$ 的 Cholesky 分解 $A$ 或 $A^T$。

        *   令 $(\zeta, \gamma) = (\xi, \eta) \Sigma^{1/2}$（或 $\Sigma^{1/2}$ 的变体），则 $(\zeta, \gamma) \sim N((0, 0), \Sigma)$。

    4.  **(4) 构造 IID 指数分布：**

        *   利用 (2) 中构造的 IID 均匀分布列 $\{\eta_n\}$。

        *   使用**概率积分逆变换**：设 $F(x) = 1 - e^{-\lambda x}$（指数分布的分布函数），定义 $F^{-1}(x) = \sup\{y; F(y) < x\}$。

        *   令 $\zeta_n = F^{-1}(\eta_n)$，则 $\{\zeta_n\}$ IID 且 $\zeta_n \sim E(\lambda)$。

  

#### 12. 构造具有给定密度的二维随机变量

  

*   **习题：** 设 $p(x, y)$ 是一个严格正密度函数。构造一个二维随机变量 $(\xi, \eta) \sim p(x, y)$。

*   **重要知识点/技巧：** **条件分布的构造**、概率积分逆变换、**联合分布分解** $p(x, y) = p_\xi(x) p_{\eta|\xi}(y|x)$。

*   **解答思路：**

    1.  计算边缘分布函数 $F(x) = \int_{-\infty}^x \int_{\mathbb{R}} p(u, v) dv du$ 和条件分布函数 $F(y|x) = \int_{-\infty}^y \frac{p(x, v)}{\int_{-\infty}^\infty p(x, v) dv} dv$。

    2.  定义相应的广义逆 $F^{-1}(x)$ 和 $F^{-1}(y|x)$。

    3.  取 $U, V$ 独立同分布 $U(0, 1)$。

    4.  定义：$\xi = F^{-1}(U)$，$\eta = F^{-1}(V | F^{-1}(U))$。

    5.  证明 $P(\xi \leq x, \eta \leq y) = \int_{-\infty}^x \int_{-\infty}^y p(u, v) dv du$。 (通过变量代换和 Fubini 定理完成证明)

  

---

  

## 练习 3-3 习题解析

  

#### 1. 判断命题对错

  

*   **习题：** 判断下列命题的对错：(1) 分布函数相同的两个随机变量相等； (2) 对任意分布函数 $F$，存在随机变量 $\xi$ 使得 $F(x) = P(\xi \leq x)$； (3) 存在既不是离散型，也不是连续型的随机变量； (4) 连续型随机变量的函数一定是连续型随机变量。

*   **重要知识点/技巧：** **随机变量的定义**（作为函数）、**分布函数**、**构造性定理**、**混合型分布**。

*   **解答思路：**

    1.  **(1) 错误。**

        *   反例： $\xi$ 和 $\eta$ 具有相同的分布函数，但作为 $\Omega$ 上的函数可以不同。取 $\Omega = \{1, 2\}, P(1) = P(2) = 1/2$。定义 $\xi(1)=1, \xi(2)=-1$ 和 $\eta(1)=-1, \eta(2)=1$。它们分布相同，但 $\xi \neq \eta$。

    2.  **(2) 正确。**

        *   证明：取概率空间 $((0, 1), \mathcal{B}((0, 1)), \text{Lebesgue})$。定义 $\xi(\omega) = \inf\{t : F(t) \geq \omega\}$（$F$ 的广义逆）。

        *   则 $P(\xi \leq x) = P(\omega \leq F(x)) = F(x)$。

    3.  **(3) 正确。**

        *   例子：**混合型随机变量**。令 $B \sim \text{Bernoulli}(1/2), \eta \sim U(0, 1)$，且 $B, \eta$ 独立。

        *   定义 $\zeta = B + (1-B)\eta$。 $\zeta$ 在 $[0, 1)$ 上有连续部分，并在 $x=1$ 处有概率质量 $P(\zeta=1)=1/2$。

        *   其分布函数 $P(\zeta \leq x) = \frac{1}{2}(x + I_{\{1\}}(x))$，既非纯粹离散，也非纯粹连续。

    4.  **(4) 错误。**

        *   反例：取连续型随机变量 $\eta \sim U(0, 1)$。

        *   定义离散函数 $\xi = \lfloor 2\eta \rfloor$。

        *   $\xi$ 只能取 0 或 1， $P(\xi=0) = P(\eta \in [0, 0.5)) = 0.5$； $P(\xi=1) = P(\eta \in [0.5, 1)) = 0.5$。

        *   $\xi$ 是离散型随机变量。

  

#### 2. 复合泊松分布（分枝过程）

  

*   **习题：** 某段时间内顾客数 $N \sim \text{Poisson}(\lambda)$。每个顾客购买电视机概率为 $p$，独立。问购买电视机的人数 $X$ 为 $k$ 的概率。

*   **重要知识点/技巧：** **全概率公式**、泊松分布、二项分布。

*   **解答思路：**

    1.  设 $X$ 为购买人数。给定总顾客数 $N=n$ 时， $X$ 服从 $\text{Binomial}(n, p)$。

    2.  利用全概率公式对 $N$ 求和：

        $$P(X = k) = \sum_{n=k}^\infty P(N = n) P(X = k | N = n)$$。

    3.  代入分布：

        $$P(X = k) = \sum_{n=k}^\infty \frac{e^{-\lambda} \lambda^n}{n!} \cdot \binom{n}{k} p^k (1 - p)^{n-k}$$。

    4.  化简：通过令 $m = n - k$ 换元，并利用 $\sum_{m=0}^\infty \frac{[\lambda(1-p)]^m}{m!} = e^{\lambda(1-p)}$。

    5.  最终结果：$P(X = k) = \frac{e^{-\lambda p} (\lambda p)^k}{k!}$。 **购买人数 $X$ 仍服从泊松分布，参数为 $\lambda p$**。

  

#### 3. 均匀分布随机变量的代数组合

  

*   **习题：** 设 $\xi, \eta, \zeta$ 独立且都是 $$ 上均匀分布。求 $W = \xi\eta$ 和 $Z = \zeta^2$ 的联合密度函数 $g(u, v)$，并证明 $P(\xi\eta < \zeta^2) = 5/9$。

*   **重要知识点/技巧：** **随机变量乘积的分布**、**联合密度**、概率积分计算。

*   **解答思路：**

    1.  **求 $W = \xi\eta$ 的分布函数 $F_W(u)$：**

        *   $F_W(u) = P(\xi\eta \leq u) = P(\eta \leq u) + P(\xi \leq u/\eta, \eta > u)$。

        *   $F_W(u) = u + \int_u^1 (u/y) dy = u + u [ \log y ]_u^1 = u(1 - \log u)$，$0 < u \leq 1$。

    2.  **求 $Z = \zeta^2$ 的密度 $p_Z(v)$：**

        *   $P(Z \leq v) = P(\zeta^2 \leq v) = P(\zeta \leq \sqrt{v}) = \sqrt{v}$。

        *   $p_Z(v) = (\sqrt{v})' = 1/(2\sqrt{v})$，$0 < v < 1$。

    3.  **求联合密度 $g(u, v)$：** 由于 $W$ 和 $Z$ 独立， $g(u, v) = p_W(u) p_Z(v)$。

        *   $p_W(u) = F_W'(u) = 1 - \log u - u \cdot (1/u) = -\log u = \log(1/u)$。

        *   $g(u, v) = \frac{\log(1/u)}{2\sqrt{v}}$，$0 < u, v < 1$。

    4.  **计算 $P(\xi\eta < \zeta^2) = P(W < Z)$：**

        *   $P(W < Z) = \iint_{0 \leq u \leq v \leq 1} g(u, v) du dv$。

        *   通过二重积分计算： $\int_0^1 \int_0^v \frac{\log(1/u)}{2\sqrt{v}} du dv$。

        *   该积分为 $\boldsymbol{5/9}$。

  

#### 4. 条件密度与联合概率计算

  

*   **习题：** 设 $p(x, y) = 6x I_{\{0<x<y<1\}}(x, y)$。求 $\xi, \eta$ 的边缘密度、条件密度 $p_{\xi|\eta}(x|y)$，以及 $P(\xi + \eta \leq 3/2)$。

*   **重要知识点/技巧：** 边缘密度、条件密度、二重积分求概率。

*   **解答思路：**

    1.  **边缘密度：**

        *   $p_\xi(x) = \int_x^1 6x dy = \boldsymbol{6x(1 - x)}$，$0 < x < 1$。

        *   $p_\eta(y) = \int_0^y 6x dx = \boldsymbol{3y^2}$，$0 < y < 1$。

    2.  **条件密度 $p_{\xi|\eta}(x|y)$：**

        *   $p_{\xi|\eta}(x|y) = p(x, y) / p_\eta(y) = \frac{6x}{3y^2} = \boldsymbol{\frac{2x}{y^2}}$，$0 < x < y$。

    3.  **概率 $P(\xi + \eta \leq 3/2)$：**

        *   积分区域 $A = \{(x, y) : 0 < x < y < 1, x + y \leq 3/2\}$。

        *   积分 $\iint_A 6x dx dy$。

        *   由于 $3/2 > 1$，区域被 $y=1$ 分割。积分分为 $y \in (0, 3/4]$ 和 $y \in (3/4, 1]$ 两部分。

        *   结果为 $0.421875 + 0.296875 = \boldsymbol{23/32}$。

  

#### 5. 构造二维正态随机变量

  

*   **习题：** 设 $\rho \in (-1, 1)$， $\xi, \eta$ 独立 $\sim U(0, 1)$， $\Phi$ 为标准正态分布函数。 (1) 证明 $(\Phi^{-1}(\xi), \Phi^{-1}(\eta)) \sim N(0, 0, 1, 1, 0)$； (2) 求矩阵 $A$ 使得 $(X, Y) := (\Phi^{-1}(\xi), \Phi^{-1}(\eta)) A \sim N(0, 0, 1, 1, \rho)$； (3) 求 $P(X > 0, Y > 0)$。

*   **重要知识点/技巧：** **概率积分逆变换**、**正态分布的线性变换**、**二维正态概率**。

*   **解答思路：**

    1.  **(1) 证明独立标准正态性：**

        *   利用逆变换 $P(\Phi^{-1}(\xi) \leq t) = P(\xi \leq \Phi(t)) = \Phi(t)$。

        *   因此 $\Phi^{-1}(\xi) \sim N(0, 1)$。由独立性， $(\Phi^{-1}(\xi), \Phi^{-1}(\eta)) \sim N(0, I_2)$ (独立标准正态)。

    2.  **(2) 求矩阵 $A$：**

        *   设 $(Z_1, Z_2) = (\Phi^{-1}(\xi), \Phi^{-1}(\eta))$. $Cov(Z_1, Z_2) = I_2$。

        *   要求 $Cov((Z_1, Z_2)A) = A^T I_2 A = A^T A = \Sigma = \begin{pmatrix} 1 & \rho \\ \rho & 1 \end{pmatrix}$。

        *   取 $A$ 为 $\Sigma$ 的 Cholesky 分解（或满足 $A^T A = \Sigma$ 的矩阵）。

        *   取 $A = \begin{pmatrix} 1 & \rho \\ 0 & \sqrt{1 - \rho^2} \end{pmatrix}$。

        *   $(X, Y) = (Z_1, \rho Z_1 + \sqrt{1-\rho^2} Z_2)$。

    3.  **(3) 求 $P(X > 0, Y > 0)$：**

        *   $P(X > 0, Y > 0) = \int_0^\infty \int_0^\infty f_{X, Y}(x, y) dx dy$。

        *   通过变量代换 $u=x, v=\frac{y-\rho x}{\sqrt{1-\rho^2}}$ 将积分转化为 $N(0, I_2)$ 的积分。

        *   转化为极坐标后，积分区域对应于角度 $\theta \in (-\arcsin \rho, \pi/2)$。

        *   结果为 $\frac{1}{2\pi} (\frac{\pi}{2} + \arcsin \rho) = \boldsymbol{\frac{1}{4} + \frac{1}{2\pi} \arcsin \rho}$。

  

#### 6. Gamma 分布的线性组合与比值

  

*   **习题：** 设 $\xi \sim \Gamma(\alpha_1, \beta), \eta \sim \Gamma(\alpha_2, \beta)$ 独立。令 $U = \xi + \eta, V = \xi/\eta$。求 $(U, V)$ 的联合密度 $g(x, y)$，并判断 $U$ 与 $V$ 的独立性。

*   **重要知识点/技巧：** **Gamma 分布**、**变量变换法**、**独立性验证**。

*   **解答思路：**

    1.  **变换关系和雅可比行列式：** $u = x + y, v = x/y$。逆变换：$x = \frac{uv}{1+v}, y = \frac{u}{1+v}$。

        *   雅可比行列式 $J = \det \begin{pmatrix} \frac{\partial x}{\partial u} & \frac{\partial x}{\partial v} \\ \frac{\partial y}{\partial u} & \frac{\partial y}{\partial v} \end{pmatrix} = -\frac{u}{(1+v)^2}$。

    2.  **联合密度 $g(u, v)$：**

        *   $f_{\xi, \eta}(x, y) = \frac{\beta^{\alpha_1+\alpha_2}}{\Gamma(\alpha_1)\Gamma(\alpha_2)} x^{\alpha_1-1} y^{\alpha_2-1} e^{-\beta(x+y)}$。

        *   代入变换和 $|J|$：

            $$g(u, v) = f_{\xi, \eta}(x(u, v), y(u, v)) \cdot \frac{u}{(1+v)^2}$$。

        *   经过化简，得到：

            $$g(u, v) = \left[ \frac{\beta^{\alpha_1+\alpha_2}}{\Gamma(\alpha_1+\alpha_2)} u^{\alpha_1+\alpha_2-1} e^{-\beta u} \right] \cdot \left[ \frac{\Gamma(\alpha_1+\alpha_2)}{\Gamma(\alpha_1)\Gamma(\alpha_2)} \frac{v^{\alpha_1-1}}{(1+v)^{\alpha_1+\alpha_2}} \right]$$。

    3.  **独立性：** 联合密度 $g(u, v)$ 可以分解为两个关于 $u$ 和 $v$ 的函数的乘积 $g(u, v) = g_U(u) g_V(v)$。

        *   $U = \xi + \eta$ 服从 $\Gamma(\alpha_1+\alpha_2, \beta)$ 分布。

        *   $V = \xi/\eta$ 服从 $\text{Beta}(\alpha_1, \alpha_2)$ 分布的变换形式。

        *   因此 **$U$ 与 $V$ 相互独立**。

  

#### 7. 均匀分布下最大值指示函数的渐近性质

  

*   **习题：** 设 $\xi_1, \dots, \xi_n$ IID $\sim U$。定义 $A_k = \{\xi_k = \max\{\xi_1, \dots, \xi_k\}\}$。 (1) 计算 $P(A_k), P(A_i A_j)$ ($i < j$)； (2) 证明 $\lim_{n\to\infty} \frac{1}{\log n} \sum_{k=1}^n P(A_k) = 1$ 和 $\lim_{n\to\infty} \frac{1}{(\log n)^2} \sum_{1 \leq i < j \leq n} P(A_i A_j) = 1/2$。

*   **重要知识点/技巧：** **次序统计量**、**示性函数期望**、**调和级数的渐近性质**。

*   **解答思路：**

    1.  **(1) 计算 $P(A_k)$ 和 $P(A_i A_j)$：**

        *   $P(A_k)$： $\xi_k$ 是 $\xi_1, \dots, \xi_k$ 中最大的概率。由对称性， $P(A_k) = 1/k$。

        *   $P(A_i A_j)$ ($i < j$)： $P(A_i A_j) = P(A_i) P(A_j | A_i)$。

            *   $P(A_j | A_i)$：给定 $A_i$ 成立， $\xi_i$ 是 $\xi_1, \dots, \xi_i$ 中最大的。现在考虑 $\xi_1, \dots, \xi_j$ 中 $\xi_j$ 是最大值。由于 $\xi_{i+1}, \dots, \xi_j$ 相对独立， $A_j$ 仅依赖于 $\xi_i, \dots, \xi_j$ 的相对大小。

            *   $P(A_i A_j) = \frac{1}{i} \cdot \frac{1}{j}$。

    2.  **(2) 证明极限：**

        *   **第一个极限：** $\sum_{k=1}^n P(A_k) = \sum_{k=1}^n 1/k$。利用调和级数渐近性质 $\sum_{k=1}^n 1/k \sim \log n$。

            $$\lim_{n\to\infty} \frac{1}{\log n} \sum_{k=1}^n P(A_k) = \lim_{n\to\infty} \frac{\sum_{k=1}^n 1/k}{\log n} = \boldsymbol{1}$$。

        *   **第二个极限：** $\sum_{1 \leq i < j \leq n} P(A_i A_j) = \sum_{1 \leq i < j \leq n} \frac{1}{i j}$。

            $$\sum_{1 \leq i < j \leq n} \frac{1}{i j} = \frac{1}{2} \left[ (\sum_{k=1}^n \frac{1}{k})^2 - \sum_{k=1}^n \frac{1}{k^2} \right]$$

            当 $n \to \infty$ 时， $\sum 1/k \sim \log n$， $\sum 1/k^2$ 是常数 $\pi^2/6$。

            $$\lim_{n\to\infty} \frac{1}{(\log n)^2} \sum_{i<j} \frac{1}{i j} = \lim_{n\to\infty} \frac{1}{2(\log n)^2} (\log n)^2 = \boldsymbol{1/2}$$。

  

#### 8. 构造具有给定密度的二维随机变量（一般情形）

  

*   **习题：** 设 $p(x, y)$ 是一个严格正密度函数。定义 $F(x) = \int_{-\infty}^x \int_{\mathbb{R}} p(u, v) dv du$ 和 $F(y|x) = \int_{-\infty}^y \frac{p(x, v)}{\int_{-\infty}^\infty p(x, v) dv} dv$。 $F^{-1}(x)$ 和 $F^{-1}(y|x)$ 为其反函数。设 $U, V$ 独立同分布 $U(0, 1)$。定义 $\xi = F^{-1}(U), \eta = F^{-1}(V | F^{-1}(U))$。证明： $(\xi, \eta) \sim p(x, y)$。

*   **重要知识点/技巧：** **概率积分逆变换法**、**联合分布函数**、全期望公式、变量代换。

*   **解答思路：**

    1.  所求目标是证明 $P(\xi \leq x, \eta \leq y) = \int_{-\infty}^x \int_{-\infty}^y p(u, v) dv du$。

    2.  利用 $\xi = F^{-1}(U)$，事件 $\{\xi \leq x\}$ 等价于 $\{U \leq F(x)\}$。

    3.  利用 $\eta = F^{-1}(V | F^{-1}(U))$，事件 $\{\eta \leq y\}$ 等价于 $\{V \leq F(y | \xi)\}$。

    4.  $P(\xi \leq x, \eta \leq y) = P(U \leq F(x), V \leq F(y | \xi))$。

    5.  由于 $U, V$ 独立 $U(0, 1)$，并且 $V$ 的边界 $F(y | \xi)$ 是关于 $U$ 的函数，可以利用 $U, V$ 的联合均匀密度（等于 1）积分：

        $$P(\cdot) = \int_{0 \leq u \leq F(x)} F(y | F^{-1}(u)) du$$。

    6.  令 $z = F^{-1}(u)$，则 $u = F(z)$， $du = F'(z) dz$。

        $$P(\cdot) = \int_{-\infty}^x F(y|z) F'(z) dz$$。

    7.  将 $F(y|z)$ 和 $F'(z)$ 的定义代入，得到：

        $$P(\cdot) = \int_{-\infty}^x \left( \int_{-\infty}^y p(z, v) dv \right) dz$$。

  

---