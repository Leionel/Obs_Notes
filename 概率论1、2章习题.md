
## 第 1 章习题解析 (练习 1-1, 1-2)

### 练习 1-1 习题解析


#### 1. 事件的集合表示

  

*   **习题：** 设 $A, B, C$ 是三个事件。用它们给出下列事件的表示：(1) 至少有一个事件发生；(2) 至少有两个事件发生；(3) 所有事件都不发生；(4) 至多一个事件发生；(5) 至多两个事件发生；(6) 恰有两个事件发生。

*   **重要知识点/技巧：** **集合运算**（并 $\cup$、交 $\cap$、补 $\bar{A}$）、事件的逻辑关系。

*   **解答思路：**

    1.  (1) **至少有一个事件发生：** $A \cup B \cup C$。

    2.  (2) **至少有两个事件发生：** $AB \cup BC \cup CA$。

    3.  (3) **所有事件都不发生：** $\bar{A} \bar{B} \bar{C}$。

    4.  (4) **至多一个事件发生：** $\bar{A}\bar{B} \cup \bar{B}\bar{C} \cup \bar{C}\bar{A}$ （即事件 (2) 的补集 $\overline{AB \cup BC \cup CA}$，或者表示为 $\bar{A}\bar{B}\bar{C} \cup A\bar{B}\bar{C} \cup \bar{A}B\bar{C} \cup \bar{A}\bar{B}C$。*注：资料中给出的答案 $\overline{AB \cup BC \cup CA}$ 与 (5) 的答案冲突，此处根据逻辑，应是 $\bar{A}\bar{B} \cup \bar{B}\bar{C} \cup \bar{C}\bar{A}$。*）

    5.  (5) **至多两个事件发生：** $\overline{ABC}$ （至少一个事件不发生）。

    6.  (6) **恰有两个事件发生：** $A B \bar{C} \cup A \bar{B} C \cup \bar{A} B C$。

  

#### 2. 钥匙开锁的概率

  

*   **习题：** 信封里有 8 把钥匙，其中只有一把能打开房门，现取出钥匙试开。分有放回和无放回两种情形，求 3 次以内能打开房门的概率。

*   **重要知识点/技巧：** **古典概率**、**对立事件**、**有放回抽样**（独立性）、**无放回抽样**（排列）。

*   **解答思路：**

    1.  **有放回情形：** 每次试开成功的概率 $p = 1/8$。3 次以内能打开房门的对立事件是“三次都失败”。

        *   $P(\text{三次都失败}) = q^3 = (7/8)^3 = 343/512$。

        *   $P(\text{成功}) = 1 - 343/512 = 169/512$。

    2.  **无放回情形：** “3 次以内能打开房门”等价于“正确钥匙在前 3 把钥匙中”。

        *   由于正确钥匙在 8 个位置上是等可能的，故概率为 $3/8$。

  

#### 3. 罐子抽球与第一个红球出现的位置

  

*   **习题：** 一个罐子包含 $b$ 个蓝色球和 $r$ 个红色球。它们被随机抽出。证明抽出的第一个红球是抽出的第 $(k+1)$ 个球的概率等于 $\binom{r+b-k-1}{r-1} / \binom{r+b}{b}$。求最后一个抽到的球是红色的概率。

*   **重要知识点/技巧：** **超几何分布**、**组合计数**、**对称性原理**。

*   **解答思路：**

    1.  **证明概率：**

        *   总排列数：在 $r+b$ 个位置中选择 $b$ 个位置放蓝球，总数为 $\binom{r+b}{b}$。

        *   有利事件数：要求前 $k$ 个位置全是蓝球（需 $k \leq b$），且第 $k+1$ 个位置是红球。剩下的 $r+b-k-1$ 个位置中，需要放置 $b-k$ 个蓝球和 $r-1$ 个红球。

        *   有利事件数 $=\binom{r+b-k-1}{b-k} = \binom{r+b-k-1}{r-1}$。

        *   概率 $P = \binom{r+b-k-1}{r-1} / \binom{r+b}{b}$。

    2.  **最后一个球是红色的概率：**

        *   由于所有红球在排列中的位置分布是对称的。

        *   最后一个位置是红球的概率等于任一个特定位置是红球的概率，即 $r/(r+b)$。

  

#### 4. 圆桌坐人的不相邻概率

  

*   **习题：** 设 $1 \leq n \leq m$. $m$ 个男孩和 $n$ 个女孩随机地沿着圆桌坐下。试求任意两个女孩都不相邻的概率。

*   **重要知识点/技巧：** **圆排列**、**插空法**、排列组合。

*   **解答思路：**

    1.  **总排列数：** $m+n$ 个人的圆排列数为 $(m+n-1)!$。

    2.  **有利事件数：**

        *   先安排 $m$ 个男孩坐成圆圈： $(m-1)!$ 种不同安排。

        *   男孩坐好后，在圆桌上有 $m$ 个空隙（每两个男孩之间一个空隙）。

        *   选择 $n$ 个空隙放置女孩： $\binom{m}{n}$ 种选择。

        *   女孩彼此不同，在选定的 $n$ 个空隙中排列： $n!$ 种排列。

        *   有利事件数 $=(m-1)! \times \binom{m}{n} \times n! = (m-1)! \cdot \frac{m!}{(m-n)!}$。

    3.  **所求概率：** $P = \frac{(m-1)! m! / (m-n)!}{(m+n-1)!}$。

        *   化简后 $P = \prod_{k=0}^{n-1} \frac{m-k}{m+n-1-k}$。

  

#### 5. 构成三角形的概率

  

*   **习题：** 在区间 $(0, 1)$ 中随机地取出三个数。求以它们作为三条线段的长度，这些线段能连接成一个三角形的概率。

*   **重要知识点/技巧：** **几何概率**、**三角形不等式** ($x+y>z, y+z>x, z+x>y$)。

*   **解答思路：**

    1.  样本空间：点 $(x, y, z)$ 在单位立方体 $(0, 1)^3$ 中均匀分布，总体积为 1。

    2.  计算不构成三角形的概率 $P(\text{不构成}) = P(x+y \leq z \text{ 或 } y+z \leq x \text{ 或 } z+x \leq y)$。

    3.  这三个事件是**互斥**的。

    4.  计算 $P(x+y \leq z)$：固定 $z \in (0, 1)$，在 $xy$ 平面单位正方形中，$x+y \leq z$ 的区域面积为 $z^2/2$。

    5.  $P(x+y \leq z) = \int_0^1 (z^2/2) dz = 1/6$。

    6.  由对称性，另外两个互斥事件的概率也都是 $1/6$。

    7.  $P(\text{不构成}) = 1/6 + 1/6 + 1/6 = 1/2$。

    8.  $P(\text{构成三角形}) = 1 - 1/2 = 1/2$。

  

#### 6. 平行弦长度大于 $R$ 的概率

  

*   **习题：** 半径为 $R$ 的圆内画平行弦，如果这些弦与垂直于这些弦的直径的交点在该直径上的位置是等可能的，求：任意画的弦的长度大于 $R$ 的概率？

*   **重要知识点/技巧：** **几何概率**、**均匀分布假设**（交点在直径上的位置等可能）。

*   **解答思路：**

    1.  设弦垂直于 $y$ 轴直径。弦由其中点 $M=(0, y)$ 的 $y$ 坐标决定， $y \in [-R, R]$。

    2.  弦长 $L(y) = 2 \sqrt{R^2 - y^2}$。

    3.  条件“弦长大于 $R$”要求 $2 \sqrt{R^2 - y^2} > R$。

    4.  解不等式得 $|y| < \sqrt{3}/2 R$。

    5.  由于中点 $y$ 在直径 $[-R, R]$ 上是等可能分布的，所求概率为有利区间的长度与总直径长度的比值。

        $$P = \frac{\sqrt{3} R}{2R} = \frac{\sqrt{3}}{2}$$。

  

#### 7. 概率不等式关系

  

*   **习题：** 假设事件 $A_r$, $1 \leq r \leq n$, 至少有一个会发生，但肯定不会超过两个发生。如果 $P(A_r) = p$ 且 $P(A_r \cap A_s) = q, r \neq s$，则证明 $p \geq 1/n$ 和 $q \leq 2/n$。

*   **重要知识点/技巧：** **容斥原理**（加法公式）、概率的非负性。

*   **解答思路：**

    1.  **条件转换：** “至少有一个发生” $P(\cup_{r=1}^n A_r) = 1$；“肯定不会超过两个发生”意味着 $P(\cup_{1 \leq r < s \leq n} A_r \cap A_s) = \sum_{1 \leq r < s \leq n} P(A_r \cap A_s)$。

    2.  **第一个不等式 $p \geq 1/n$：**

        *   由容斥原理（仅保留前两项）：$P(\cup A_r) = \sum P(A_r) - \sum_{r<s} P(A_r \cap A_s) = 1$。

        *   $1 = np - \binom{n}{2} q = np - \frac{n(n-1)}{2} q$。

        *   解出 $p = \frac{1}{n} + \frac{n-1}{2} q$。由于 $q \geq 0$（概率非负性），故 $\boldsymbol{p \geq 1/n}$。

    3.  **第二个不等式 $q \leq 2/n$：**

        *   考虑事件 $E = (\cup_{r=1}^n A_r) \setminus (\cup_{1 \leq r < s \leq n} A_r \cap A_s)$。由于 $E$ 是单事件发生（恰好一个发生），则 $P(E) \geq 0$。

        *   $P(E) = P(\cup A_r) - P(\cup_{r<s} A_r \cap A_s) = 1 - \binom{n}{2} q = 1 - \frac{n(n-1)}{2} q$。

        *   $1 - \frac{n(n-1)}{2} q \geq 0$，解得 $q \leq \frac{2}{n(n-1)}$。*注：资料中给出了不同的推导 $np - n(n-1)q \geq 0$，这是假设 $\sum P(A_r) - \sum P(A_r A_s) \geq 0$（即 $P(\cup A_r)$ 的下界），这导致 $p \geq \frac{n-1}{n} q$。将 $p$ 代入 $1 = np - \frac{n(n-1)}{2} q$ 并化简，最终得到 $\boldsymbol{q \leq 2/n}$。*

  

#### 8. 考签未被抽到的概率

  

*   **习题：** 设 $n \geq N$. 考试时共有 $N$ 张考签, $n$ 个学生参加考试, 被抽过的考签立刻放回。求在考试结束之后, 至少有一张考签没有被抽到的概率。

*   **重要知识点/技巧：** **容斥原理**、独立放回抽样。

*   **解答思路：**

    1.  设 $A$ 表示“至少有一张考签没有被抽到”。 $A_i$ 表示“第 $i$ 张考签没有被抽到”。

    2.  $A = \cup_{i=1}^N A_i$。

    3.  对固定的 $k$ 张考签 $i_1, \dots, i_k$，一个学生抽不到其中任何一张的概率为 $1 - k/N$。

    4.  由于抽取是独立且放回的， $n$ 个学生都抽不到这 $k$ 张考签的概率为：

        $$P(A_{i_1} \cap \cdots \cap A_{i_k}) = (1 - k/N)^n$$。

    5.  由容斥原理，所求概率为 $P(A) = \sum_{k=1}^N (-1)^{k-1} \sum_{1 \leq i_1 < \dots < i_k \leq N} P(A_{i_1} \cap \cdots \cap A_{i_k})$。

    6.  由于对称性，对于每个 $k$，有 $\binom{N}{k}$ 个这样的交集，因此：

        $$P(A) = \sum_{k=1}^N (-1)^{k-1} \binom{N}{k} (1 - k/N)^n$$。

  

#### 9. 混合概率空间的验证

  

*   **习题：** 设 $(\Omega, \mathcal{F}_1, P_1), (\Omega, \mathcal{F}_2, P_2)$ 是两个概率空间，且 $\alpha \in$。令 $\mathcal{F} = \mathcal{F}_1 \cap \mathcal{F}_2$，且 $P(A) = \alpha P_1(A) + (1-\alpha) P_2(A), A \in \mathcal{F}$。证明 $(\Omega, \mathcal{F}, P)$ 也是一个概率空间。

*   **重要知识点/技巧：** **概率空间的三公理**（$\sigma$-代数、非负性、规范性、可列可加性）。

*   **解答思路：**

    1.  **验证 $\mathcal{F}$ 是 $\sigma$-代数：**

        *   (a) $\Omega \in \mathcal{F}_1, \Omega \in \mathcal{F}_2 \Rightarrow \Omega \in \mathcal{F}$。

        *   (b) 补运算封闭：若 $A \in \mathcal{F}$，则 $\bar{A} \in \mathcal{F}_1, \bar{A} \in \mathcal{F}_2 \Rightarrow \bar{A} \in \mathcal{F}$。

        *   (c) 可数并封闭：若 $A_n \in \mathcal{F}$，则 $\cup A_n \in \mathcal{F}_1, \cup A_n \in \mathcal{F}_2 \Rightarrow \cup A_n \in \mathcal{F}$。

    2.  **验证 $P$ 是概率测度：**

        *   (a) **非负性：** $P_1(A) \geq 0, P_2(A) \geq 0, \alpha \in$，故 $P(A) \geq 0$。

        *   (b) **规范性：** $P(\Omega) = \alpha P_1(\Omega) + (1-\alpha) P_2(\Omega) = \alpha \cdot 1 + (1-\alpha) \cdot 1 = 1$。

        *   (c) **可列可加性：** 设 $A_n \in \mathcal{F}$ 两两不交。

            $$P(\cup_{n=1}^\infty A_n) = \alpha P_1(\cup_{n=1}^\infty A_n) + (1-\alpha) P_2(\cup_{n=1}^\infty A_n)$$。

            利用 $P_1, P_2$ 的可列可加性，展开为 $\sum_{n=1}^\infty [\alpha P_1(A_n) + (1-\alpha) P_2(A_n)] = \sum_{n=1}^\infty P(A_n)$。

  

### 练习 1-2 习题解析

  

#### 1. 立方体顶点染色的存在性

  

*   **习题：** 球体表面的 10% 是蓝色的，其余是红色的。证明：无论颜色的分布方式如何，都可以切成一个立方体 $S$，其所有顶点都是红色的。

*   **重要知识点/技巧：** **概率法**、**布尔不等式** (Boole's inequality)。

*   **解答思路：**

    1.  设 $A$ 为球体总面积，蓝色区域 $B$ 面积为 $0.1A$。

    2.  考虑内接立方体的 8 个顶点 $V_1, \dots, V_8$。

    3.  任一顶点 $V_i$ 落在蓝色区域的概率 $P(V_i \in B) = \frac{\text{蓝色面积}}{\text{球面总面积}} = 0.1$。

    4.  令 $A_i$ 为事件 $V_i \in B$。事件 $A = \cup_{i=1}^8 A_i$ 表示“至少有一个顶点是蓝色”。

    5.  由布尔不等式（次可加性）：$P(A) = P(\cup A_i) \leq \sum P(A_i) = 8 \times 0.1 = 0.8$。

    6.  事件“立方体的所有顶点都是红色”是 $A$ 的补集。

    7.  $P(\text{所有顶点都是红色}) = 1 - P(A) \geq 1 - 0.8 = 0.2$。

    8.  由于 $P(\cdot) > 0$，因此存在至少一个内接立方体，其所有顶点都是红色的。

  

#### 2. $\sigma$-代数的简单刻画

  

*   **习题：** 设 $\Omega = \mathbb{R}$，$\mathcal{F}$ 是由 $\{\{x\}, x \in \mathbb{R}\}$ 生成的 $\sigma$-代数。请给出 $\mathcal{F}$ 的一个简单刻画。

*   **重要知识点/技巧：** **$\sigma$-代数的生成**、**可数集**、**余可数集**。

*   **解答思路：**

    1.  $\mathcal{F}$ 必须包含所有单点集 $A = \{\{x\} : x \in \mathbb{R}\}$。

    2.  通过可数并，$\mathcal{F}$ 必须包含**所有可数集** $C = \cup_{x \in C} \{x\}$。

    3.  通过补运算，$\mathcal{F}$ 必须包含所有可数集的补集（**余可数集**）。

    4.  定义 $\mathcal{G} = \{A \subset \mathbb{R} : A \text{ 是可数集，或 } A^c \text{ 是可数集}\}$。

    5.  验证 $\mathcal{G}$ 满足 $\sigma$-代数的三个条件（包含 $\emptyset$，对补运算封闭，对可数并封闭）。

    6.  最小性证明：任何包含 $A$ 的 $\sigma$-代数都必须包含 $\mathcal{G}$。

    7.  因此，$\mathcal{F}$ 是**所有可数集或余可数集**构成的集合族。

  

#### 3. 分布函数的性质与跳跃点集

  

*   **习题：** 设 $P$ 是可测空间 $(\mathbb{R}, \mathcal{B}(\mathbb{R}))$ 上的一个概率。令 $F(x) = P((-\infty, x]), x \in \mathbb{R}$。(1) 验证 $F$ 是单调的，右连续且左极限存在。(2) 对 $n \geq 1$，令 $A_n = \{x; F(x) - F(x-) \geq 1/n\}, A = \{x; F(x) - F(x-) > 0\}$。证明 $A_n$ 是有限集，$A$ 至多可数，且 $A_n \uparrow A$。

*   **重要知识点/技巧：** **分布函数的基本性质**、**概率测度的连续性**（上连续性和下连续性）、**分布函数的跳跃点**。

*   **解答思路：**

    1.  **(1) 验证性质：**

        *   (a) **单调性：** 若 $x \leq y$，则 $(-\infty, x] \subset (-\infty, y]$，由概率测度的单调性知 $F(x) \leq F(y)$。

        *   (b) **右连续性：** 设 $h_n \downarrow 0$，定义 $B_n = (-\infty, x+h_n]$。则 $B_n \downarrow (-\infty, x]$。由概率测度的上连续性， $\lim F(x+h_n) = P((-\infty, x]) = F(x)$。

        *   (c) **左极限存在：** 设 $h_n \downarrow 0$，定义 $C_n = (-\infty, x-h_n]$。则 $C_n \uparrow (-\infty, x)$。由下连续性， $\lim F(x-h_n) = P((-\infty, x)) = F(x-)$。

    2.  **(2) 跳跃点集：**

        *   (a) **$A_n$ 是有限集：** 假设 $A_n$ 是无限集，则存在无穷多个点 $x_i$ 使得 $F(x_i) - F(x_i-) \geq 1/n$。由于 $F(x)$ 有界 ($F(x) \leq 1$)，这些跳跃的总和必须有限。因此， $A_n$ 中点的个数 $k$ 必须满足 $k/n \leq F(M) \leq 1$，即 $k \leq n$。故 $A_n$ 只能是有限集。

        *   (b) **$A$ 至多可数：** $A$ 是所有跳跃点构成的集合。由于 $A = \cup_{n=1}^\infty A_n$。$A$ 是可数多个有限集的并，因此 $A$ **至多可数**。

        *   (c) **$A_n \uparrow A$：** 显然 $A_n \subset A_{n+1} \subset A$。若 $x \in A$，则 $F(x) - F(x-) = d > 0$。取 $n$ 使得 $1/n < d$，则 $x \in A_n$，故 $A = \cup_{n=1}^\infty A_n$。

  

#### 4. 概率的上极限不等式

  

*   **习题：** 设 $(\Omega, \mathcal{F}, P)$ 是一个概率空间，且 $\{A_n, n \geq 1\} \subset \mathcal{F}$。证明 $\limsup_{n\to\infty} P(A_n) \leq P(\limsup_{n\to\infty} A_n)$。

*   **重要知识点/技巧：** **集合的上极限** $\limsup A_n = \cap_{n=1}^\infty \cup_{k=n}^\infty A_k$、概率测度的上连续性（对单调下降序列）。

*   **解答思路：**

    1.  定义 $B_n = \cup_{k=n}^\infty A_k$。则 $B_n \supset B_{n+1}$，且 $\cap_{n=1}^\infty B_n = \limsup_{n\to\infty} A_n$。

    2.  由概率测度的上连续性： $\lim_{n\to\infty} P(B_n) = P(\limsup A_n)$。

    3.  对于每个 $n$， $A_n \subset B_n$。由概率的单调性 $P(A_n) \leq P(B_n)$。

    4.  对上式取上极限： $\limsup P(A_n) \leq \limsup P(B_n)$。

    5.  由于 $\{P(B_n)\}$ 是单调下降序列，其上极限等于极限： $\limsup P(B_n) = \lim P(B_n) = P(\limsup A_n)$。

    6.  因此 $\limsup P(A_n) \leq P(\limsup A_n)$。

  

#### 5. 离散测度的紧支撑与紧子集逼近

  

*   **习题：** 设 $\mu$ 是 $[0, +\infty)$ 上的一个有限测度， $\mu(\{\frac{k+1}{n}\}) = \frac{1}{k^2 n^2}$，且 $\mu((\cup_{k, n} \{\frac{k+1}{n}\})^c) = 0$。 (1) 找最小闭集 $F \subset [0, \infty)$，使得 $\mu(F^c) = 0$。(2) 证明：对每个集 $A \subset F$，存在一列有界闭集（紧集） $\{F_n\} \subset A$，使得 $\lim \mu(F_n) = \mu(A)$。

*   **重要知识点/技巧：** **测度的支撑集**（最小闭集 $F$）、**有限测度的紧正则性**（紧集逼近）。

*   **解答思路：**

    1.  **(1) 最小闭集 $F$：**

        *   测度 $\mu$ 集中在 $E = \{\frac{k+1}{n} : k \geq 1, n \geq 1\}$ 上。

        *   最小闭集 $F$ 必须是 $E$ 的闭包 $\bar{E}$。

        *   由于 $k + 1/n \to k$ 当 $n \to \infty$，故 $F$ 必须包含所有整数 $k \geq 1$。

        *   因此 $F = \bar{E} = \{\frac{k+1}{n} : k \geq 1, n \geq 1\} \cup \{k : k \geq 1\}$。

    2.  **(2) 紧集逼近：**

        *   首先验证 $\mu$ 是**有限测度**： $\mu([0, \infty)) = \sum_{k=1}^\infty \sum_{n=1}^\infty \frac{1}{k^2 n^2} = (\sum 1/k^2)^2 = \frac{\pi^4}{36} < \infty$。

        *   由于 $\mu$ 是有限测度，且测度集中在 $F$ 上。定义 $F_m = \{\frac{k+1}{n} : 1 \leq k \leq m, 1 \leq n \leq m\} \cup \{1, 2, \dots, m\}$。

        *   $F_m$ 是**有限集**，故 $F_m$ 是紧集。

        *   设 $A \subset F$。定义 $K_m = A \cap F_m$。

        *   $K_m$ 是紧集，且 $K_m \subset A$。

        *   由于 $\cup_{m=1}^\infty F_m = F$，所以 $K_m = A \cap F_m \uparrow A \cap F = A$。

        *   由测度的**下连续性**： $\lim_{m\to\infty} \mu(K_m) = \mu(A)$。故 $K_m$ 为所求紧集序列。

  

#### 6. $\pi$-$\lambda$ 定理证明独立性

  

*   **习题：** 设 $(\Omega, \mathcal{F}, P)$ 是一个概率空间，且 $\mathcal{C}_1 \subset \mathcal{F}, \mathcal{C}_2 \subset \mathcal{F}$ 是两个 $\pi$ 类。假设对任意 $A \in \mathcal{C}_1$ 和 $B \in \mathcal{C}_2$， $P(AB) = P(A)P(B)$。 (1) 证明 $G_2$ 是 $\lambda$-类，且 $G_2 = \sigma(\mathcal{C}_2)$。(2) 证明 $G_1$ 是 $\lambda$-类，且 $G_1 = \sigma(\mathcal{C}_1)$。(3) 证明：对任意 $A \in \sigma(\mathcal{C}_1)$ 和 $B \in \sigma(\mathcal{C}_2)$, $P(AB) = P(A)P(B)$。

*   **重要知识点/技巧：** **$\pi-\lambda$ 定理**、**$\lambda$-类**的三个条件（$\Omega$ 属于，差运算封闭，单调上升极限封闭）。

*   **解答思路：**

    1.  **(1) 证明 $G_2$ 是 $\lambda$-类且 $G_2 = \sigma(\mathcal{C}_2)$：**

        *   **$\lambda$-类验证：** 验证 $G_2 = \{B \in \sigma(\mathcal{C}_2); P(AB) = P(A)P(B) \text{ 对任意 } A \in \mathcal{C}_1\}$。

            *   (a) $\Omega \in G_2$： $P(A\Omega) = P(A) = P(A)P(\Omega)$。

            *   (b) 差运算封闭：若 $B_1 \subset B_2 \in G_2$，则 $P(A(B_2 \setminus B_1)) = P(AB_2) - P(AB_1) = P(A)(P(B_2) - P(B_1)) = P(A)P(B_2 \setminus B_1)$。

            *   (c) 上升极限封闭：若 $B_n \in G_2, B_n \uparrow B$，则 $P(AB) = \lim P(AB_n) = \lim P(A)P(B_n) = P(A)P(B)$。

        *   **$G_2 = \sigma(\mathcal{C}_2)$：** $\mathcal{C}_2$ 是 $\pi$ 类且 $\mathcal{C}_2 \subset G_2$。由 $\pi-\lambda$ 定理，$G_2 \supset \sigma(\mathcal{C}_2)$。又由定义 $G_2 \subset \sigma(\mathcal{C}_2)$。故 $G_2 = \sigma(\mathcal{C}_2)$。

    2.  **(2) 证明 $G_1$ 是 $\lambda$-类且 $G_1 = \sigma(\mathcal{C}_1)$：**

        *   **$\lambda$-类验证：** 类似 (1) 证明 $G_1 = \{A \in \sigma(\mathcal{C}_1); P(AB) = P(A)P(B) \text{ 对任意 } B \in G_2\}$ 满足 $\lambda$-类的三个条件。

        *   **$G_1 = \sigma(\mathcal{C}_1)$：** 由于 $G_2 = \sigma(\mathcal{C}_2)$，对任意 $A \in \mathcal{C}_1$ 和 $B \in G_2$， $P(AB) = P(A)P(B)$。故 $\mathcal{C}_1 \subset G_1$。由 $\pi-\lambda$ 定理， $G_1 = \sigma(\mathcal{C}_1)$。

    3.  **(3) 证明结论：** 由 $G_1 = \sigma(\mathcal{C}_1)$ 和 $G_2 = \sigma(\mathcal{C}_2)$，且 $G_1$ 的定义保证了对任意 $A \in G_1$ 和 $B \in G_2$， $P(AB) = P(A)P(B)$。

  

---

  

## 第 2 章习题解析 (练习 2-1, 2-2, 2-3)

  

### 练习 2-1 习题解析

  

#### 1. 条件概率与全概率比较

  

*   **习题：** 假设有三个事件 $A, B, C$。(1) 已知 $P(A | C) > P(B | C)$ 和 $P(A | C^c) > P(B | C^c)$。问是否一定有 $P(A) > P(B)$？(2) 已知 $P(A | C) > P(A | C^c)$ 和 $P(B | C) > P(B | C^c)$。问是否一定有 $P(A) > P(B)$？

*   **重要知识点/技巧：** **全概率公式** $P(A) = P(A|C)P(C) + P(A|C^c)P(C^c)$。

*   **解答思路：**

    1.  **(1) 结论成立。**

        *   $P(A) - P(B) = P(C) [P(A|C) - P(B|C)] + P(C^c) [P(A|C^c) - P(B|C^c)]$。

        *   由于 $P(A|C) > P(B|C)$ 和 $P(A|C^c) > P(B|C^c)$，且 $P(C), P(C^c) > 0$，所以 $P(A) - P(B) > 0$。

    2.  **(2) 结论不一定成立。**

        *   **反例：** 设 $P(C) = 0.8$。

        *   $P(A|C) = 0.6 > P(A|C^c) = 0.4$ (满足第一个条件)。

        *   $P(B|C) = 0.95 > P(B|C^c) = 0.5$ (满足第二个条件)。

        *   计算 $P(A) = 0.6 \times 0.8 + 0.4 \times 0.2 = 0.56$。

        *   计算 $P(B) = 0.95 \times 0.8 + 0.5 \times 0.2 = 0.86$。

        *   此时 $P(A) < P(B)$。

  

#### 2. 三人比赛的优胜者概率

  

*   **习题：** 甲乙丙三人进行某项比赛，三人胜每局的概率相等（1/3）。先胜三局者为优胜者。若甲胜了第一、三局，乙胜了第二局，求丙成了整场比赛优胜者的概率。

*   **重要知识点/技巧：** **条件概率**、**概率树**、**独立重复试验**。

*   **解答思路：**

    1.  当前比分：甲 2 胜，乙 1 胜，丙 0 胜。

    2.  比赛从第 4 局开始，丙需要连胜 3 局（第 4、5、6 局）才能达到 3 胜，因为甲和乙当前获胜局数都 $\leq 2$。

    3.  **情形 1：** 丙在第 4、5、6 局连胜。

        *   $P(\text{丙获胜}) = (1/3) \times (1/3) \times (1/3) = 1/27$。

    4.  **资料中的复杂分析：** 资料似乎考虑了更多局数（甚至到第 7 局），但这似乎是不必要的，因为甲乙在第 6 局之前即可获胜。*注：根据资料中提供的最终答案和计算过程 $3(1/3)^4 + (1/3)^3 = 2/27$，我们采用其提供的结果，但其过程描述较为混乱，实际应为只有丙连胜 3 局才能获胜 $P(\text{丙获胜}) = (1/3)^3$。*

    5.  **最终结论（按资料）：** 丙获胜的概率为 $3 (1/3)^4 + (1/3)^3 = 2/27$。

  

#### 3. 次品率计算

  

*   **习题：** 某工厂有甲,乙,丙 3 个车间，产量分别占全厂的 25%, 35%, 40%，次品率分别为 5%, 4%, 2%。求全厂产品的次品率。

*   **重要知识点/技巧：** **全概率公式**。

*   **解答思路：**

    1.  设 $A_i$ 为产品来自车间 $i$， $B$ 为次品。

        *   $P(A_1)=0.25, P(A_2)=0.35, P(A_3)=0.40$。

        *   $P(B|A_1)=0.05, P(B|A_2)=0.04, P(B|A_3)=0.02$。

    2.  全厂次品率 $P(B) = \sum P(A_i) P(B|A_i)$。

    3.  $P(B) = 0.25 \times 0.05 + 0.35 \times 0.04 + 0.40 \times 0.02 = 0.0125 + 0.0140 + 0.0080 = 0.0345$。

    4.  全厂产品的次品率为 $\boldsymbol{3.45\%}$。

  

#### 4. 迷路问路与贝叶斯公式

  

*   **习题：** 公园游客 (2/3) 答对概率 $3/4$，说谎者 (1/3) 总是答错。设出口为东或西，概率 $1/2$。

    *   (a) 问一次，答东。东正确的概率？

    *   (b) 问两次，都答东。证明：东正确的概率为 $1/2$。

    *   (c) 问三次，都答东。东正确的概率？

    *   (d) 问四次，都答东。证明：东正确的概率为 $27/70$。

    *   (e) 问四次，前三次东，第四次西。证明：东正确的概率为 $9/10$。

*   **重要知识点/技巧：** **贝叶斯公式**、全概率公式、独立性。

*   **解答思路：**

    1.  设 $E$ 为出口在东。 $T$ 为游客 (答对 $3/4$)， $L$ 为说谎者 (答错 1)。 $P(T)=2/3, P(L)=1/3, P(E)=1/2$。

    2.  **(a) 答东 $R_1$。 $P(E|R_1)$：**

        *   $P(R_1|E) = P(T) \cdot 3/4 + P(L) \cdot 0 = 1/2$。

        *   $P(R_1|E^c) = P(T) \cdot 1/4 + P(L) \cdot 1 = 1/2$。

        *   $P(R_1) = P(E)P(R_1|E) + P(E^c)P(R_1|E^c) = 1/2$。

        *   $P(E|R_1) = \frac{P(E)P(R_1|E)}{P(R_1)} = \frac{1/2 \cdot 1/2}{1/2} = \boldsymbol{1/2}$。

    3.  **(b) 答两次东 $R_1R_2$。 $P(E|R_1R_2)$：**

        *   $P(R_1R_2|E) = P(T) \cdot (3/4)^2 = 3/8$。

        *   $P(R_1R_2|E^c) = P(T) \cdot (1/4)^2 + P(L) \cdot 1 = 3/8$。

        *   $P(E|R_1R_2) = \frac{P(E)P(R_1R_2|E)}{P(R_1R_2)} = \frac{1/2 \cdot 3/8}{1/2 \cdot 3/8 + 1/2 \cdot 3/8} = \boldsymbol{1/2}$。

    4.  **(c) 答三次东 $R_1R_2R_3$。 $P(E|R_1R_2R_3)$：**

        *   $P(R_1R_2R_3|E) = P(T) \cdot (3/4)^3 = 9/32$。

        *   $P(R_1R_2R_3|E^c) = P(T) \cdot (1/4)^3 + P(L) \cdot 1 = 11/32$。

        *   $P(E|R_1R_2R_3) = \frac{1/2 \cdot 9/32}{1/2 \cdot 9/32 + 1/2 \cdot 11/32} = \boldsymbol{9/20}$。

    5.  **(d) 答四次东 $R_1R_2R_3R_4$。 $P(E|R_1R_2R_3R_4)$：**

        *   $P(R_1R_2R_3R_4|E) = P(T) \cdot (3/4)^4 = 27/128$。

        *   $P(R_1R_2R_3R_4|E^c) = P(T) \cdot (1/4)^4 + P(L) \cdot 1 = 43/128$。

        *   $P(E|R_1R_2R_3R_4) = \frac{1/2 \cdot 27/128}{1/2 \cdot 27/128 + 1/2 \cdot 43/128} = \boldsymbol{27/70}$。

    6.  **(e) 前三次东，第四次西 $A$。 $P(E|A)$：**

        *   $P(A|E) = P(T) \cdot (3/4)^3 \cdot 1/4 = 9/128$。

        *   $P(A|E^c) = P(T) \cdot (1/4)^3 \cdot 3/4 = 1/128$。

        *   $P(E|A) = \frac{1/2 \cdot 9/128}{1/2 \cdot 9/128 + 1/2 \cdot 1/128} = \boldsymbol{9/10}$。

  

#### 5. 随机掷骰子的条件概率

  

*   **习题：** $N$ 是随机正整数， $P(N=i) = 2^{-i}$。 $S$ 是掷 $N$ 次骰子的点数之和， $D$ 是第一次掷出的点数。求 (a) $P(N=2|S=4)$； (b) $P(S=4|N \text{为偶数})$； (c) $P(N=2|S=4, D=1)$。

*   **重要知识点/技巧：** **贝叶斯公式**、**全概率公式**、**几何级数求和**。

*   **解答思路：**

    1.  **准备工作：** $P(N=i) = 2^{-i}$。

        *   $P(S=4|N=i)$： $S=4$ 在 $i$ 次掷骰子中出现的概率。

        *   $i=1: P(S=4|N=1) = 1/6$。

        *   $i=2: P(S=4|N=2) = 3/36 = 1/12$ (和为 4 的组合: (1, 3), (2, 2), (3, 1))。

        *   $i=3: P(S=4|N=3) = 3/216 = 1/72$ (和为 4 的组合: (1, 1, 2) 及其排列，共 3 种)。

        *   $i=4: P(S=4|N=4) = 1/1296$ (和为 4 的组合: (1, 1, 1, 1))。

        *   $i \geq 5: P(S=4|N=i) = 0$。

    2.  **(a) $P(N=2|S=4)$：** 使用贝叶斯公式。

        *   分母 $P(S=4) = \sum_{i=1}^\infty P(S=4|N=i) P(N=i)$。

        *   $P(S=4) = 1/6 \cdot 1/2 + 1/12 \cdot 1/4 + 1/72 \cdot 1/8 + 1/1296 \cdot 1/16 = 2197/20736$。

        *   分子 $P(S=4, N=2) = P(S=4|N=2) P(N=2) = 1/12 \cdot 1/4 = 1/48 = 432/20736$。

        *   $P(N=2|S=4) = \frac{432}{2197}$。

    3.  **(b) $P(S=4|N \text{为偶数})$：**

        *   $P(N \text{为偶数}) = \sum_{k=1}^\infty P(N=2k) = \sum_{k=1}^\infty (1/4)^k = (1/4)/(1-1/4) = 1/3$。

        *   $P(S=4|N \text{为偶数}) = \frac{P(S=4, N \text{为偶数})}{P(N \text{为偶数})} = 3 \sum_{k=1}^\infty P(S=4|N=2k) P(N=2k)$。

        *   由于 $N \geq 5$ 时 $P(S=4|N=i)=0$，只需考虑 $k=1, 2$。

        *   $P(S=4|N \text{为偶数}) = 3 \cdot [P(S=4|N=2)P(N=2) + P(S=4|N=4)P(N=4)]$。

        *   $P(S=4|N \text{为偶数}) = 3 \cdot [1/12 \cdot 1/4 + 1/1296 \cdot 1/16] = 433/6912$。

    4.  **(c) $P(N=2|S=4, D=1)$：** $D=1$ 表示第一次掷出 1。

        *   分子 $P(N=2, S=4, D=1)$。

        *   $P(N=2, S=4, D=1) = P(N=2) P(S=4, D=1|N=2) = 1/4 \cdot 1/6 = 1/24$。 (S=4, D=1意味着第二次掷出 3)

        *   分母 $P(S=4, D=1) = \sum_{i=1}^\infty P(N=i) P(S=4, D=1|N=i)$。

        *   $P(S=4, D=1|N=i)$ 仅对 $i=2, 3, 4$ 非零。

        *   $P(S=4, D=1) = 1/4 \cdot 1/6 + 1/8 \cdot 1/18 + 1/16 \cdot 1/216 = 169/3456$。

        *   $P(N=2|S=4, D=1) = \frac{1/24}{169/3456} = \boldsymbol{144/169}$。

  

### 练习 2-2 习题解析

  

#### 1. 独立事件与概率 0 或 1

  

*   **习题：** 证明：(1) 若事件 $A$ 与自身独立，则 $P(A) = 0$ 或 1。(2) 若 $P(A) = 0$ 或 1，则 $A$ 与任意事件独立。

*   **重要知识点/技巧：** **独立性定义** $P(A \cap B) = P(A)P(B)$、概率的单调性。

*   **解答思路：**

    1.  **(1) $A$ 与自身独立：**

        *   $P(A \cap A) = P(A)P(A)$。

        *   由于 $A \cap A = A$，所以 $P(A) = [P(A)]^2$。

        *   $P(A) [1 - P(A)] = 0$，故 $P(A) = 0$ 或 $P(A) = 1$。

    2.  **(2) $P(A) = 0$ 或 1 蕴含独立性：** 需证明 $P(A \cap B) = P(A)P(B)$ 对任意 $B$ 成立。

        *   **若 $P(A)=0$：** $A \cap B \subset A$，由单调性 $P(A \cap B)=0$。$P(A)P(B) = 0 \cdot P(B) = 0$。

        *   **若 $P(A)=1$：** $P(A^c)=0$。$A^c \cap B \subset A^c$，故 $P(A^c \cap B)=0$。

        *   $P(A \cap B) = P(B) - P(A^c \cap B) = P(B)$。而 $P(A)P(B) = 1 \cdot P(B) = P(B)$。

  

#### 2. 三孩和四孩家庭的独立性

  

*   **习题：** 设 $A = \{\text{至多有一个女孩}\}, B = \{\text{男女孩都有}\}$。证明：对 3 孩之家，$A$ 和 $B$ 独立；对 4 孩之家，$A$ 和 $B$ 不独立。

*   **重要知识点/技巧：** **古典概率**、独立性检验 $P(A \cap B) = P(A)P(B)$。

*   **解答思路：**

    1.  **三孩之家 (样本空间大小 8)：**

        *   $P(A) = P(0 \text{ 女}) + P(1 \text{ 女}) = 1/8 + 3/8 = 1/2$。

        *   $P(B) = 1 - P(\text{全男}) - P(\text{全女}) = 1 - 2/8 = 3/4$。

        *   $A \cap B = P(\text{恰好 } 1 \text{ 女}) = 3/8$。

        *   $P(A)P(B) = 1/2 \cdot 3/4 = 3/8 = P(A \cap B)$。故**独立**。

    2.  **四孩之家 (样本空间大小 16)：**

        *   $P(A) = P(0 \text{ 女}) + P(1 \text{ 女}) = 1/16 + 4/16 = 5/16$。

        *   $P(B) = 1 - P(\text{全男}) - P(\text{全女}) = 1 - 2/16 = 14/16 = 7/8$。

        *   $A \cap B = P(\text{恰好 } 1 \text{ 女}) = 4/16 = 1/4$。

        *   $P(A)P(B) = 5/16 \cdot 7/8 = 35/128$。 $P(A \cap B) = 1/4 = 32/128$。

        *   $35/128 \neq 32/128$。故**不独立**。

  

#### 3. $\pi$-类与 $\sigma$-代数的独立性

  

*   **习题：** 设 $A, B, C$ 为**互相独立的事件**。令 $\mathcal{C}_1 = \{\emptyset, A, B, AB\}, \mathcal{C}_2 = \{\emptyset, C\}$。证明 $\mathcal{C}_1, \mathcal{C}_2$ 是两个 $\pi$ 类，且 $\sigma(\{A, B\})$ 与 $\sigma(\{C\})$ 独立。

*   **重要知识点/技巧：** **$\pi$-类定义**（对有限交封闭）、**乘法族独立性**（$\pi-\lambda$ 定理）。

*   **解答思路：**

    1.  **证明 $\mathcal{C}_1, \mathcal{C}_2$ 是 $\pi$ 类：** 验证其所有元素的交集仍在其自身中。

    2.  **证明 $\mathcal{C}_1$ 与 $\mathcal{C}_2$ 独立：** 需证对任意 $X \in \mathcal{C}_1, Y \in \mathcal{C}_2$， $P(X \cap Y) = P(X)P(Y)$。

        *   当 $Y = \emptyset$ 时，显然成立。

        *   当 $Y = C$ 时，需要验证 $P(X \cap C) = P(X)P(C)$。

            *   若 $X=A$ 或 $X=B$，由 $A, B, C$ 互相独立，成立。

            *   若 $X=AB$， $P(AB \cap C) = P(ABC)$。由 $A, B, C$ 互相独立 $P(ABC) = P(A)P(B)P(C)$。同时 $P(AB)P(C) = P(A)P(B)P(C)$。成立。

    3.  **结论：** 由于 $\mathcal{C}_1, \mathcal{C}_2$ 是 $\pi$ 类且互相独立，由 $\pi-\lambda$ 定理，它们生成的 $\sigma$-代数 $\sigma(\mathcal{C}_1)$ 与 $\sigma(\mathcal{C}_2)$ 独立。 $\sigma(\mathcal{C}_1) = \sigma(\{A, B\}), \sigma(\mathcal{C}_2) = \sigma(\{C\})$。

  

#### 4. 有放回抽样的概率空间

  

*   **习题：** 设一批产品的次品率为 $p$。写出对其作 $n$ 次有放回的抽样检查所对应的概率空间。

*   **重要知识点/技巧：** **概率空间的三要素** $(\Omega, \mathcal{F}, P)$、**伯努利试验**、独立抽样。

*   **解答思路：**

    1.  **样本空间 $\Omega$：** 每次抽样结果为 0（正品）或 1（次品）。 $n$ 次有放回抽样的样本空间为所有长度为 $n$ 的 0-1 序列的集合： $\Omega = \{(\omega_1, \dots, \omega_n) : \omega_i \in \{0, 1\}\}$。总数为 $2^n$。

    2.  **$\sigma$-代数 $\mathcal{F}$：** 由于 $\Omega$ 是有限集，取 $\mathcal{F}$ 为 $\Omega$ 的幂集 $\mathcal{F} = 2^\Omega$。

    3.  **概率测度 $P$：** 设次品率为 $p$。对于任一样本点 $\omega = (\omega_1, \dots, \omega_n)$，次品出现次数 $k = \sum \omega_i$。

        *   由于是独立抽样， $P(\{\omega\}) = p^k q^{n-k}$。

        *   对于任意事件 $A \subset \Omega$： $P(A) = \sum_{\omega \in A} P(\{\omega\})$。

  

#### 5. 和数为 6 先于 7 的概率

  

*   **习题：** 独立地重复投掷两枚骰子，并计算点数之和。求“和数为 6”出现在“和数为 7”之前的概率。

*   **重要知识点/技巧：** **几何分布**、**条件概率**、**吸收壁**模型。

*   **解答思路：**

    1.  **计算基本概率：** 总结果 36 种。

        *   $A$ (和为 6)：(1, 5), (2, 4), (3, 3), (4, 2), (5, 1)， $P(A) = a = 5/36$。

        *   $B$ (和为 7)：(1, 6), (2, 5), (3, 4), (4, 3), (5, 2), (6, 1)， $P(B) = b = 6/36$。

    2.  **建立方程：** 设 $p$ 为 $A$ 先于 $B$ 出现的概率。考虑第一次投掷：

        *   概率 $a$ 出现 $A$ (成功，概率 1)。

        *   概率 $b$ 出现 $B$ (失败，概率 0)。

        *   概率 $1-a-b$ 出现其它结果 (重新开始，概率 $p$)。

        *   $p = a \cdot 1 + b \cdot 0 + (1-a-b) \cdot p$。

    3.  **求解：** $p = a / (a+b)$。

        *   $p = (5/36) / (5/36 + 6/36) = \boldsymbol{5/11}$。

  

#### 6. 偶数个正面的概率递推

  

*   **习题：** 反复抛出一枚非均匀硬币，正面概率 $p$。设 $p_n$ 是 $n$ 抛掷后出现偶数个正面的概率 ($p_0=1$)。用全概率公式建立关于 $p_n$ 的递推公式，并求 $p_n$。

*   **重要知识点/技巧：** **全概率公式**、**线性递推关系**。

*   **解答思路：**

    1.  **建立递推公式：** 考虑第 $n$ 次抛掷的结果。

        *   第 $n$ 次是反面（概率 $q=1-p$）：前 $n-1$ 次必须是偶数个正面，概率 $p_{n-1}$。

        *   第 $n$ 次是正面（概率 $p$）：前 $n-1$ 次必须是奇数个正面，概率 $1-p_{n-1}$。

        *   $p_n = q p_{n-1} + p (1 - p_{n-1}) = \boldsymbol{p + (1 - 2p) p_{n-1}}$。

    2.  **求解 $p_n$：**

        *   平衡值 $x = p + (1-2p)x$，解得 $x = 1/2$。

        *   令 $d_n = p_n - 1/2$，则 $d_n = (1-2p) d_{n-1}$。

        *   初始条件 $d_0 = p_0 - 1/2 = 1/2$。

        *   $d_n = (1/2) (1-2p)^n$。

        *   $p_n = \boldsymbol{1/2 + 1/2 (1-2p)^n}$。

  

#### 7. 赌庄破产概率

  

*   **习题：** 赌庄资产 100,000 元（10 单位），赌徒赌资无限。每次压注 10,000 元（1 单位），赢钱概率 $p$。问 $p$ 为多大时赌徒使赌庄破产的概率大于 $2/3$？

*   **重要知识点/技巧：** **随机游走**、**赌徒破产问题**（吸收壁）、递推公式求解。

*   **解答思路：**

    1.  **建模：** 赌庄初始资金 $x=10$。赌庄破产概率 $\phi(x)$ 满足 $\phi(x) = p \phi(x-1) + q \phi(x+1)$。

    2.  **求解公式：** 当 $p > 0.5$ 时，解为 $\phi(x) = (q/p)^x = (\frac{1-p}{p})^x$。当 $p \leq 0.5$ 时，$\phi(x) = 1$。

    3.  **应用条件 $\phi(10) > 2/3$：**

        *   **若 $p \leq 0.5$：** $\phi(10) = 1 > 2/3$，满足条件。

        *   **若 $p > 0.5$：** 需 $(\frac{1-p}{p})^{10} > 2/3$。

        *   解得 $p < 1 / (1 + (2/3)^{1/10})$。

        *   由于 $(2/3)^{1/10} \approx 0.9604$，所以 $p < 1/1.9604 \approx 0.5101$。

    4.  **结论：** 赌徒能使赌庄破产的概率大于 $2/3$ 的条件是 $p \in (0, 0.5101)$。

  

#### 8. 排版错误与泊松逼近

  

*   **习题：** 书有 $10^6$ 个符号，排错率 $10^{-4}$。校对改正率 $0.9$。求校对后错误不多于 5 个的概率的近似值。

*   **重要知识点/技巧：** **泊松逼近**（二项分布 $B(n, p)$ 趋近 $Po(np)$）、**复合泊松过程**。

*   **解答思路：**

    1.  **初次排错 $\xi$：** $\xi \sim B(10^6, 10^{-4})$。由于 $n$ 大 $p$ 小，$E(\xi) = \lambda_1 = 10^6 \cdot 10^{-4} = 100$。 $\xi$ 近似服从 $Po(100)$。

    2.  **校对后剩余错误 $\eta$：** 给定 $\xi=l$ 个错误，每个错误以 $1-0.9=0.1$ 的概率未被改正。

        *   $\eta | \xi=l \sim B(l, 0.1)$。

        *   $\eta$ 服从复合泊松分布，参数为 $\lambda_2 = \lambda_1 \cdot (1-0.9) = 100 \cdot 0.1 = 10$。

    3.  **近似计算 $P(\eta \leq 5)$：** $\eta$ 近似服从 $Po(10)$。

        *   $$P(\eta \leq 5) \approx \sum_{k=0}^5 P(\eta=k) = \sum_{k=0}^5 \frac{10^k}{k!} e^{-10}$$。

        *   计算近似值约为 $\boldsymbol{0.0671}$。

  

#### 9. 汽车座位数与正态逼近

  

*   **习题：** 100 人乘 A, B 两车，乘 A 车概率 $0.4$，乘 B 车概率 $0.6$。问为了以 $99\%$ 概率保证每个人有座位，两车分别至少设置多少个座位？

*   **重要知识点/技巧：** **二项分布**、**DeMoivre-Laplace 极限定理**（正态逼近）。

*   **解答思路：**

    1.  设 $\eta$ 表示乘 A 车的人数。 $\eta \sim B(100, 0.4)$。 $E(\eta)=40, D(\eta) = 100 \cdot 0.4 \cdot 0.6 = 24$。 $\sqrt{D(\eta)} = \sqrt{24}$。

    2.  设 A, B 车座位数分别为 $k_1, k_2$。需求 $P(\eta \leq k_1, 100-\eta \leq k_2) \geq 0.99$。

    3.  $P(\eta \leq k_1, 100-\eta \leq k_2) = P(100-k_2 \leq \eta \leq k_1)$。

    4.  通过 DeMoivre-Laplace 极限定理近似为 $\Phi(\frac{k_1-40}{\sqrt{24}}) - \Phi(\frac{100-k_2-40}{\sqrt{24}}) = \Phi(\frac{k_1-40}{\sqrt{24}}) - \Phi(\frac{60-k_2}{\sqrt{24}})$。

    5.  利用对称性，我们取 $P(\eta \leq k_1) \geq 0.995$ 且 $P(100-\eta \leq k_2) \geq 0.995$。

        *   $P(\eta \leq k_1) \geq 0.995 \Rightarrow \Phi(\frac{k_1-40}{\sqrt{24}}) \geq 0.995$。

        *   $P(100-\eta \leq k_2) \geq 0.995 \Rightarrow P(\eta \geq 100-k_2) \geq 0.995$。

    6.  查表 $\Phi(2.58) \approx 0.995$。

        *   A 车： $\frac{k_1-40}{\sqrt{24}} \geq 2.58 \Rightarrow k_1 \geq 40 + 2.58 \sqrt{24} \approx 52.6$。 $k_1 \geq 53$。

        *   B 车： $\frac{k_2-60}{\sqrt{24}} \geq 2.58$。 $k_2 \geq 60 + 2.58 \sqrt{24} \approx 72.6$。 $k_2 \geq 73$。

    7.  A 车至少 53 个座位，B 车至少 73 个座位。

  

### 练习 2-3 习题解析

  

#### 1. 智力竞赛的后验概率

  

*   **习题：** 甲, 乙, 丙抢答，概率 $0.2, 0.3, 0.5$。答对概率 $0.9, 0.4, 0.4$。已知题已答对，问谁答对的可能性最大。

*   **重要知识点/技巧：** **全概率公式**、**贝叶斯公式**。

*   **解答思路：**

    1.  设 $A, B, C$ 为抢到答题权， $D$ 为答对。

    2.  $P(D) = P(A)P(D|A) + P(B)P(D|B) + P(C)P(D|C)$。

        *   $P(D) = 0.2 \times 0.9 + 0.3 \times 0.4 + 0.5 \times 0.4 = 0.18 + 0.12 + 0.20 = 0.5$。

    3.  计算后验概率 $P(A|D), P(B|D), P(C|D)$。

        *   $P(A|D) = P(A)P(D|A)/P(D) = 0.18 / 0.5 = 0.36$。

        *   $P(B|D) = P(B)P(D|B)/P(D) = 0.12 / 0.5 = 0.24$。

        *   $P(C|D) = P(C)P(D|C)/P(D) = 0.20 / 0.5 = 0.40$。

    4.  **结论：** 丙答对的可能性最大 (0.4)。

  

#### 2. 随机掷骰子的最大点数分布

  

*   **习题：** $N$ 是随机正整数， $P(N=i) = 2^{-i}$。 $M$ 表示掷出的最大点数。对 $1 \leq r \leq 6$，求 $P(M \leq r)$ 和 $P(M=r)$。

*   **重要知识点/技巧：** **全概率公式**、**极值分布**（最大值 $M$）、几何级数求和。

*   **解答思路：**

    1.  **计算 $P(M \leq r)$：**

        *   $P(M \leq r) = \sum_{j=1}^\infty P(M \leq r | N=j) P(N=j)$。

        *   给定 $N=j$，每次掷骰子点数 $\leq r$ 的概率为 $r/6$。 $P(M \leq r | N=j) = (r/6)^j$。

        *   $P(M \leq r) = \sum_{j=1}^\infty (r/6)^j (1/2)^j = \sum_{j=1}^\infty (r/12)^j$。

        *   这是首项 $r/12$，公比 $r/12$ 的几何级数，求和得 $\frac{r/12}{1 - r/12} = \frac{r}{12-r}$。

    2.  **计算 $P(M = r)$：**

        *   $P(M=r) = P(M \leq r) - P(M \leq r-1)$。

        *   $P(M=r) = \frac{r}{12-r} - \frac{r-1}{12-(r-1)} = \frac{r(13-r) - (r-1)(12-r)}{(12-r)(13-r)}$。

  

#### 3. 独立事件的充要条件

  

*   **习题：** 证明：事件 $A_1, \dots, A_n$ 独立的充要条件是 $2^n$ 个等式成立： $P(\hat{A}_1 \cdots \hat{A}_n) = P(\hat{A}_1) \cdots P(\hat{A}_n)$，其中 $\hat{A}_i$ 取 $A_i$ 或 $A_i^c$。

*   **重要知识点/技巧：** **独立性的定义**、**数学归纳法**、**分解事件**。

*   **解答思路：**

    1.  **必要性 $(\Rightarrow)$：** 假设 $A_1, \dots, A_n$ 独立。使用归纳法。

        *   设对 $n$ 成立。证明 $n+1$ 时， $B_1, \dots, B_{n-1}$ 独立，其中 $B_n = A_n A_{n+1}$。

        *   利用 $P(\hat{A}_1 \cdots \hat{A}_{n+1})$ 可以通过 $P(\hat{A}_1 \cdots \hat{A}_{n-1} \hat{A}_n \hat{A}_{n+1})$ 展开，最终证明 $\prod P(\hat{A}_i)$ 成立。

    2.  **充分性 $(\Leftarrow)$：** 假设 $2^n$ 个等式成立。

        *   为证明 $A_1, \dots, A_{n+1}$ 独立，只需证明对任意 $1 \leq j_1 < \dots < j_k \leq n+1$ 的指标集， $P(\hat{A}_{j_1} \cdots \hat{A}_{j_k}) = \prod P(\hat{A}_{j_i})$。

        *   利用已知等式 $P(\hat{A}_1 \cdots \hat{A}_{n+1}) = \prod P(\hat{A}_i)$，通过对不包含的事件求和：

            $$P(\hat{A}_1 \cdots \hat{A}_{j-1} \hat{A}_{j+1} \cdots \hat{A}_{n+1}) = P(\hat{A}_1 \cdots \hat{A}_{j-1} A_j \hat{A}_{j+1} \cdots \hat{A}_{n+1}) + P(\hat{A}_1 \cdots \hat{A}_{j-1} A_j^c \hat{A}_{j+1} \cdots \hat{A}_{n+1})$$。

        *   利用假设展开并相加，最终归纳到低阶的独立性。

  

#### 4. 3 次有放回抽样的概率空间

  

*   **习题：** 设一批产品的次品率为 $0.01$。写 3 次有放回的抽样检查所对应的概率空间。

*   **重要知识点/技巧：** **伯努利概型**（独立试验）。

*   **解答思路：**

    1.  **概率空间 $(\Omega, \mathcal{F}, P)$：**

    2.  **样本空间 $\Omega$：** 3 次试验，结果为 0 或 1。 $\Omega = \{(x_1, x_2, x_3) : x_i \in \{0, 1\}\}$。

    3.  **$\sigma$-代数 $\mathcal{F}$：** $\mathcal{F}$ 是 $\Omega$ 的所有子集 (幂集)。

    4.  **概率测度 $P$：** 设次品率 $p=0.01, q=0.99$。对任一样本点 $\omega=(x_1, x_2, x_3)$，次品数 $k = x_1+x_2+x_3$。

        $$P(\{\omega\}) = (0.01)^k (0.99)^{3-k}, x_i \in \{0, 1\}$$。

  

#### 5. 赌徒输赢的概率

  

*   **习题：** 某赌徒每次押一元赌注，输赢概率同为 $1/2$。若输了 50 元或赢了 25 元他就停止赌博。求他在停止赌博时，输了 50 元或赢了 25 元的概率。

*   **重要知识点/技巧：** **对称随机游走**、**吸收壁**问题。

*   **解答思路：**

    1.  这是一个随机游走问题。设赌徒初始资金在 $x=50$。

    2.  输了 50 元 $\iff$ 资金到达 0 (吸收壁)。

    3.  赢了 25 元 $\iff$ 资金到达 $50+25=75$ (吸收壁)。

    4.  由于输赢概率均为 $1/2$ (对称游走)，赌徒输光（输 50 元）的概率为 $\frac{\text{赢钱所需的步数}}{\text{总距离}} = 25 / (50+25) = \boldsymbol{1/3}$。

    5.  赌徒赢钱（赢 25 元）的概率为 $\frac{\text{输钱所需的步数}}{\text{总距离}} = 50 / (50+25) = \boldsymbol{2/3}$。

  

#### 6. 排版错误的概率估算

  

*   **习题：** 书有 $10^6$ 个符号，排错率 $10^{-4}$，校对改正率 $0.9$。求：(1) 校对前错误在 80 到 120 之间的概率。(2) 校对后错误不多于 15 个的概率。(3) 经过两次校对后还有错误的概率。

*   **重要知识点/技巧：** **DeMoivre-Laplace 极限定理**（正态逼近）、**泊松逼近**、**复合泊松分布**。

*   **解答思路：**

    1.  **初次排错 $\xi$：** $\xi \sim B(10^6, 10^{-4})$。 $E(\xi)=100, \sigma(\xi) \approx 10$。

    2.  **(1) $P(80 \leq \xi \leq 120)$：** 利用 DeMoivre-Laplace 极限定理：

        *   $P(80 \leq \xi \leq 120) \approx \Phi(\frac{120-100}{10}) - \Phi(\frac{80-100}{10}) = \Phi(2) - \Phi(-2) = 2\Phi(2) - 1$。

        *   $P(\cdot) \approx \boldsymbol{0.9544}$。

    3.  **校对后剩余错误 $\eta$：** $\eta$ 近似服从 $Po(10)$。

    4.  **(2) $P(\eta \leq 15)$：**

        *   $P(\eta \leq 15) \approx \sum_{k=0}^{15} \frac{10^k}{k!} e^{-10}$。

        *   $P(\cdot) \approx \boldsymbol{0.943}$。

    5.  **(3) 两次校对后还有错误的概率 $P(\zeta \geq 1)$：**

        *   第一次校对后剩余 $\eta \sim Po(10)$。

        *   第二次校对的改正率 $p' = 0.9$，未改正率 $q' = 0.1$。

        *   第二次校对后剩余错误 $\zeta$ 服从参数为 $\lambda_{new} = \lambda_\eta \cdot (1-0.9) = 10 \cdot 0.1 = 1$ 的泊松分布。*注：此处资料答案采用了不同的模型 $\lambda_{new} = 10 \cdot 0.001 \approx 0.001$，这与习题 2-1.8 的设定不一致，但我们根据资料计算。*

        *   $P(\zeta \geq 1) = 1 - P(\zeta = 0) = 1 - e^{-\lambda_{new}}$。

        *   根据资料公式 $1 - \sum_{k=0}^\infty P(\zeta=0|\eta=k)P(\eta=k) = 1 - e^{-0.001}$。 $\boldsymbol{P(\zeta \geq 1) \approx 0.001}$。

  

#### 7. 汽车座位的等概率选择

  

*   **习题：** 100 人乘 A, B 两车，等可能（概率 0.5）选择 A, B。问为了以 $99\%$ 概率保证每个人有座位，每车至少设置多少个座位？

*   **重要知识点/技巧：** **DeMoivre-Laplace 极限定理**。

*   **解答思路：**

    1.  设 $\eta$ 表示乘 A 车人数。 $\eta \sim B(100, 0.5)$。 $E(\eta)=50, \sigma(\eta) = \sqrt{100 \cdot 0.5 \cdot 0.5} = 5$。

    2.  设每车座位 $k$。需求 $P(100-k \leq \eta \leq k) \geq 0.99$。

    3.  利用对称性 $P(100-k \leq \eta \leq k) \approx 2\Phi(\frac{k-50}{5}) - 1$。

    4.  $\Phi(\frac{k-50}{5}) \geq 0.995$。

    5.  $\frac{k-50}{5} \geq 2.58$。

    6.  $k \geq 50 + 5 \times 2.58 = 62.9$。

    7.  每车至少设置 $\boldsymbol{63}$ 个座位。

  

#### 8. Borel-Cantelli 引理（独立事件）

  

*   **习题：** 设 $\{A_n, n \geq 1\}$ 独立， $P(A_n) = n^{-\alpha}, \alpha > 0$。证明：(1) 当 $\alpha > 1$ 时， $P(A_n, i.o.) = 0$。(2) 当 $\alpha \leq 1$ 时， $P(A_n, i.o.) = 1$。

*   **重要知识点/技巧：** **Borel-Cantelli 引理**（第一和第二引理）、无穷多次发生 ($A_n, i.o.$)。

*   **解答思路：**

    1.  **判定收敛性：** $\sum_{n=1}^\infty P(A_n) = \sum_{n=1}^\infty n^{-\alpha}$。

    2.  **(1) $\alpha > 1$：** $\sum P(A_n) < \infty$。

        *   由**Borel-Cantelli 第一引理**， $P(A_n, i.o.) = \boldsymbol{0}$。

    3.  **(2) $\alpha \leq 1$：** $\sum P(A_n) = \infty$。

        *   由于事件 $\{A_n\}$ 相互独立，由**Borel-Cantelli 第二引理**， $P(A_n, i.o.) = 1 - \lim_{n\to\infty} \prod_{k=n}^\infty P(A_k^c) = 1$。

        *   证明过程： $\prod_{k=n}^\infty P(A_k^c) = \prod_{k=n}^\infty (1 - 1/k^\alpha)$。

        *   当 $\sum 1/k^\alpha = \infty$ 时， $\prod (1 - 1/k^\alpha) = 0$。 $P(A_n, i.o.) = 1 - 0 = \boldsymbol{1}$。